# AI 视频剪辑智能体 · 本轮收口报告（2026-09-24 凌晨）

> 承接「再生成两段测试素材」+ 六轮后续推进。两段新素材不只是素材 —— 它们**当场照出了 2 个真 bug**；
> 随后把 ASR 的三个疑点查清，用**零新增模型下载**拿回段落级时间戳，顺势接上了**按台词删除**能力，
> 把 VAD 分段带来的 3.6× 开销优化回 3.7× 提速；再回头补修**提示词契约**（Bug Q），
> 最后**用本地真实模型首次跑通 LLM 规划路径**，发现并堵上"少删/多删不报错"（Bug R）。
> 本报告记录：新素材、5 个 bug（N/O/P/Q/R）、新能力、性能优化、真机验证、以及最终验收状态。

---

## 一、交付物：两段新测试素材

设计原则：**与已有素材互补**，专攻覆盖不到的场景；结构"一眼可判"
（切点落在整齐时刻、同一目标出现在**多个不连续区间**且各区间时长不同 —— 找"删不干净 / 区间算错"的最好探针）。

| 素材 | 规格 | 结构 | 专测什么 | 实测校验（硬测量） |
|---|---|---|---|---|
| `测试素材2_多目标.mp4` | 24s / 1280×720 / 25fps + **逐幕对齐**中文旁白 | **6 幕**交替（bus↔人物），其中 **3 个公交镜头** 0–4 / 8–12 / 16–20s | 多区间删除、六幕跳切、多目标 YOLO、**台词检索** | 切点 `[4,8,12,16,20]`；13 段静音；YOLO: bus / person / tie |
| `测试素材3_竖屏音乐.mp4` | 20s / **720×1280 竖屏** / 25fps + numpy 合成**纯音乐（无语音）** | 5 幕 + zoompan 真实推镜 | **竖屏**排版、**ASR 无语音降级**、连续音频溯源 | 切点 `[4,8,12,16]`；**0 段静音**；帧差 `2.63/0.87`（推镜真在动） |

生成脚本 `scripts/make_more_clips.py`（可重跑）。

> **两个设计决定值得记**：
> ① **幕间用硬切，不加淡入淡出** —— 加了之后 1Hz 场景检测把 1 个切点算成 2 个，6 幕被判成 12 个场景，
> 且落在黑帧上的关键帧喂给 YOLO 什么都检不出。
> ② **旁白逐幕对齐**（第 N 幕说"第 N 幕…"）—— 这个设计后来成了验证 ASR 分段的**标尺**。

---

## 二、素材照出的 2 个真 bug

### Bug N — 语义删除只删「关键帧命中跨度」，不删「整个镜头」

- **发现方式**：素材2 的 3 个公交镜头跑「删掉有bus的片段」→ 规划出 `(2.0, 2.0)` **零长度区间**。
- **双重后果**：① 同镜头残留未覆盖画面（用户看到"没删干净"）；② `delete_range` 要求 `start < end`
  → 严格模式报错，UI 非严格模式**静默跳过**（悄悄少删）。
- **修复**：命中外扩到镜头边界（边界时刻归**下一镜**，与 ffmpeg `-ss` 语义一致），相邻命中镜头合并。
- **实测**：15s 素材 → **5.00s**（旧版 8.00s，残留 3s 公交画面）；24s 素材 → **12.00s**，A/V 逐段镜像、0 警告。
- **护栏**：`test_agent.py::TestSemanticRemoveCoversWholeShot` ×7。

### Bug O — 竖屏/小分辨率下字幕横向溢出被裁

- **发现方式**：素材3 加字幕后**抽帧用眼睛看** —— 6 个汉字撑满 720px、两端各裁掉约一个字。
  横屏 1280×720 上同一份代码**看起来完全正常**，所以此前一直没暴露。**这就是竖屏素材的价值。**
- **根因**：ASS 没写 `PlayResX/PlayResY` → libass 缺省按 **384×288** 解释坐标，
  `Fontsize: 36` 实际含义变成"画面**高度**的 12.5%"、与宽度无关；叠加 `WrapStyle: 2`（不折行）。
- **修复**：声明坐标系 + `WrapStyle: 0` + 标准 V4+ 23 字段 Format；字号 `min(H×4.8%, W×5.5%)`。
- **实测**：竖屏 6 字完整落在画面内（`outputs/验证_竖屏字幕.png`）；横屏字号从 ≈90px 收敛到合理值。
- **护栏**：`test_contract.py::TestSubtitleFitsFrame` ×5。

---

## 三、Bug P — ASR：两级护栏 + 段落级时间戳 + 阈值调优

### 三个疑点的实测结论

| 假设 | 实测结论 |
|---|---|
| `sentence_timestamp=True` 能拿逐句时间戳 | ❌ **不生效**。`generate()` 只返回 `{'key','text'}`，无 `sentence_info`，也不含 `timestamp`。同一条旁白只能拿整片一条、且**幕序错乱** |
| 把 `vad_model="fsmn-vad"` 挂进 `AutoModel` 能治幻觉 / 拿时间戳 | ❌ **都不能**。它只是内部切段后又拼回一条，**段边界被吞掉** |
| 纯音乐会被转出垃圾 | ✅ 确认：`text='the'`，退化分支取整片时长 → 放大成 `0.00–20.00s "the"` |

### 修复①：VAD 分段（零新增下载拿回时间戳）

关键认知：**单独实例化** `AutoModel(model="fsmn-vad")` —— 直接给
`res[0]["value"] == [[起ms, 止ms], …]`。拿到边界后**逐段切音频、逐段转写**。

### 修复②：静音阈值 800 → 300ms

| 阈值 | 素材2（六幕旁白） | 素材3（纯音乐） |
|---|---|---|
| 默认 800ms | **3 段**（幕间停顿被合并，跨两幕） | 1 段 |
| **300ms** | **11 段**，边界落在 `4150/8150/12140/16160/20160` —— **与幕边界精确吻合** | **6 段**（反噬，见下） |

### 修复③：整体护栏（因为②带来了反噬）

调细后**纯音乐素材从 1 段变成 6 段**幻觉（`'嗯嗯' / '呜啦啦' / '呜呜呜' / '嗯' …`），
而**单段护栏全部漏过**：每段只覆盖 16% 时长（`covers_all` 不成立）、文本又含汉字
（`has_cjk` 反而成了"像真台词"的证据）。

→ 新增**整体判据** `_looks_like_silent_footage`：**同时**满足
「段数 ≥3」+「每段 ≤3 字」+「区间并集覆盖 ≥90% 时长」→ 整批丢弃。实测素材3 恢复为**空**。

### 踩过一次的坑：参数传了不等于生效

用 `vad_post_args={"max_end_silence_time": 300}` 传参时，**四个不同阈值（800/500/300/200）跑出完全相同的结果**
—— 差点据此得出"静音阈值不影响分段"的错误结论。查源码才发现：
`self.vad_opts = VADXOptions(**kwargs)` —— **认的是直接关键字**，签名里的 `vad_post_args` 从未被使用。

### 最终效果（素材2）

```
[0.08-1.28] 第一幕    [1.28-3.35] 画面里有一辆公交车
[4.15-5.27] 第二幕    [5.27-6.71] 画面里是两个人
[8.15-9.34] 第三幕    [9.34-11.28] 又出现了一辆公交车   …
```
素材3 → **转写段为空**。粒度**段落级/半句级**（非逐句，已如实标注）。

---

## 四、新增能力：按台词删除

有了带时间戳的转写段后，规划器支持「删掉**说** X 的片段」。

**关键设计：台词区间同样外扩到镜头。** 台词粒度可到"半句"（`[8.15,9.34] "第三幕"`），
直接删会只删半句、留下半个镜头 —— 与 Bug N 同一条铁律。`_speech_ranges` 把它扩成覆盖的镜头 `(8.0,12.0)`。

**事实块同步改造**：台词**按镜头聚合**后再交给 LLM
（`镜头 8.00-12.00s: "第三幕" / "又出现了一辆公交车"`），模型拿到的是**可直接使用的删除区间**，
不需要自己推演外扩（推演极易出错）。

**视觉优先、台词兜底**：「删掉**有**X的片段」先查视觉标签，视觉无匹配再回退台词检索
（解决"用户说中文『公交车』、视觉标签却是英文 bus"的错位）。

### 交叉验证（素材2，24s）

| 指令 | 走的路径 | 规划结果 | 成片 |
|---|---|---|---|
| 删掉**说**公交车的片段 | 台词检索 | `[(0,4),(8,12),(16,20)]` | **12.00s**，video=12.000000 / audio=12.000000，**0 警告** |
| 删掉**有**公交车的片段 | 视觉检测（YOLO） | `[(0,4),(8,12),(16,20)]` | 同上 |

两条**互相独立**的路径收敛到同一结果 —— 因为素材设计上"公交幕"的旁白正好也提到公交车。

**UI 已同步暴露这个能力**：界面的指令示例补上了 `删掉说"公交车"的片段`，并注明
⑤⑥ 都需要先跑 ② M2、**删除区间一律按镜头对齐**（不会只删半句/半个画面）；
`test_ui.py` 新增一条走 **UI 处理器路径**（`do_plan` → `do_apply_compile`）的用例，
断言区间外扩到镜头 —— 确保界面链路也能跑通，而不只是库函数路径。

---

## 五、性能：VAD 分段带来 3.6× 开销 → 拆计时定位真瓶颈，优化到 3.7×

分段把"整片转 1 次"变成"逐段转 11 次"，实测 **5.03s vs 基线 1.38s**。**没有猜，先把耗时拆开计时**：

| 环节 | 耗时（11 段 / 24s 素材） |
|---|---|
| 整片一次转写（基线） | 1.38s |
| 分段后：转写本身 | 1.72s |
| 分段后：**切音频（11 次 ffmpeg 进程）** | **3.30s ← 大头** |
| 合计 | 5.03s |

→ **瓶颈不在模型（只多 0.34s），在"每段起一个 ffmpeg 进程"（3.30s）。**

改法：`soundfile` 一次读成 numpy → 内存切片（0.003s）→ **批量喂给 FunASR**（`generate(input=[…])`）。

| 方案 | 耗时 |
|---|---|
| ffmpeg 切段 + 批量转写 | 5.02s |
| **内存切片 + 批量转写** | **1.36s（3.7×）** |

**安全兜底（正确性优先于速度）**：`_asr_segment_texts` **任何"对不上"就退回慢路径** ——
批量返回**条数 ≠ 输入条数**、抛异常、soundfile 不可用 → 一律退回 ffmpeg 逐条。
条数校验是硬要求：对不上意味着时间戳会**错位到别的段**，比慢一点严重得多。

实测批量返回与输入**逐条对应、内容顺序一致、文本完全一致**（`_verify/bench_batch_asr.py` / `bench_numpy_slice.py`）。
真实链路复跑结果**与优化前完全一致**，总耗时 42–51s → **33.96s**。

---

## 六、补修 Bug Q：提示词是契约，但它**从来没被测试过**

回头审「按台词删除」的提示词契约时，发现两件事**同时**成立：

1. `facts_block` 在**拿不到镜头信息**时会退化成逐条列台词（没有「镜头 X-Y」字样），
   但 schema 规则 3b **无条件**宣称「事实里台词已**按镜头聚合**」——
   **提示词在对模型说假话。** 模型会去找不存在的「镜头 X-Y」，或违反 3b 改用台词小时间戳
   → 又回到"只删半句、留下半个镜头"（Bug N 的同一类症状），**而且不报错**。
2. 提示词内容**零断言** —— stub 测试只校验 model 名 / messages 非空 / 鉴权头，
   返回什么由 stub 自己造。**事实块或规则被改坏，全部测试照样绿，LLM 路径悄悄退化。**

**修复（三处一起改）**：

| 改动 | 内容 |
|---|---|
| 退化分支自报家门 | `台词（**未按镜头聚合：没有可用的镜头信息…**）` vs 正常 `台词（**按镜头聚合：…**）`，首个标识不同，模型一眼能分辨 |
| 规则 3b 改条件式 | 「先看这行开头写着哪种，再决定用哪份数据」+ 补一个按台词删除的 worked example |
| 补两层契约测试 | **拼装层**断言 `build_prompt()` 返回值；**线上层**让 stub 存下服务端**实际收到**的请求体再断言 |

**顺带堵掉一个静默降级**：规则路径没有模型兜底 —— 没有镜头信息时 `_speech_ranges` 只能给 1–2s
半句区间，成片必然残留半个镜头，用户只会觉得"没删干净"。新增纯判定函数
`speech_delete_degrades_to_raw_segments()`，`do_plan` 状态里显式提示；判定正则与规划器
**共用** `_SPEECH_DELETE_RE`，避免两处各写一份日后分家。

**测试写法上踩的两个坑（已写进注释）**：

- **子串断言会自欺**：`"未按镜头聚合"` **包含** `"按镜头聚合"` —— 只 `assertIn("按镜头聚合")`
  会在退化场景**假通过**。→ 用带前缀的完整标识，并在正向用例补 `assertNotIn` 反向标识。
- **对整条提示词 `assertNotIn` 必然误报**：schema 的【示例】区天然含同形字符串
  （示例就该长得像真的）。→ 先按标记**切出事实段**，只在段内断言。

**实测复跑**：`_verify/verify_speech_delete.py` 与修复前**完全一致** ——
素材2 转写 11 段、两条路径同为 `[(0,4),(8,12),(16,20)]`、成片 12.00s、0 警告。

---

## 七、真机验证 → Bug R：模型会**少删/多删**，而且不报错

项目里躺着一台本地 Qwen3-0.6B（`E:\AI_models\Qwen_Model\start_qwen.bat`，OpenAI 兼容，端口 8001）。
**第一次把它接上真跑** LLM 规划路径（`_verify/verify_llm_real.py`），结果立刻暴露了 stub 测不出来的东西：

| 指令 | 模型实际输出 | 后果 |
|---|---|---|
| 删掉**说**公交车的片段 | 只给 **2 条** `(0,4)` `(8,12)` | **少删**：第 3 个公交镜头 16–20s 仍在成片里 |
| 删掉**有**公交车的片段 | 给 **4 条**，多的那条是 `(20,24)` | **多删**：把用户没要的第 6 幕也删了（8s 而非 12s） |
| 去掉前2秒 | `(trim, in=2.0)` | ✅ 正确 |

**两次错误都是「操作成功、0 警告」。** 时间轴确实改了，成片也出得来，但结果和指令对不上 ——
而此前建的所有防线（容错解析、自动重试、派生 id 别名表）**全都救不了**，因为模型给出的是**合法 JSON**。
`temperature=0` 下两次跑出**完全相同**的输出，说明这是稳定偏差，不是偶发抖动。

> **好消息也是坏消息**：模型**没有**用台词小时间戳（规则 3b + 聚合事实块在真机上确实生效了），
> 但它转而"少给它写 / 多给它编"。提示词能治的它治好了，治不了的在别处冒出来。

**根因（一句话）**：语义删除（"删掉有/说 X 的片段"）本质是**检索**，不是创作 ——
答案早就写在 YOLO 标签和 ASR 转写里了。手上有标准答案，就不该只靠"提示词写得够清楚"去赌模型照做。

**修复：新增「确定性事实核对」闸。**

```
expected_semantic_ranges(cmd, index)   ← 从 M2 事实确定性算出"这条指令该删哪些区间"
semantic_delete_discrepancies(cmd, index, ops)  ← 核对模型输出，报出 漏删 / 多删
```

容差 0.05s 吸收舍入噪声；`do_plan` 把结果挂进状态，并建议切换规则模式。
**能算出确定性标准答案时就自动纠正**（`do_plan` 把模型不一致的删除区间直接换成精确结果，
非删除类操作原样保留），同时把"漏删/多删"原样摆给用户看 —— 既保证交付物正确，又不是静默替换。
仅"无 M2 事实 / 关键词从未命中"这类算不出标准答案的情况保持只警告。

实测：三处不一致**全部拦下**（漏删 `16.00-20.00s`、多删 `20.00-24.00s`），非语义指令（`去掉前2秒`）**零误报**。

**再补一层：真模型 → 真 App 路径 → 真出片**（`_verify/verify_ui_llm_real.py`）。
前面那步走的是 provider/planner 层；这次走完整的 `do_plan` → `do_apply_compile` → 渲染 → ffprobe，
把"用户最终拿到什么"摆出来：

| 后端 | 规划区间 | **成片** | ③ 状态 |
|---|---|---|---|
| 真模型 Qwen3-0.6B | 模型给 4 条（含多出 `20-24`）→ **自动纠正为 3 条** | **12.00s**（正确） | ⚠️ **核对提示出现 + 已自动改用规则结果** |
| 规则 Planner | 3 条 | **12.00s**（正确） | 正常 |

> **差 0.00s、一致 ✅。** 修复前真模型路径拿到错的 8 秒成片（差 4.00s）；
> 修复后 `do_plan` 在规划阶段就把区间自动纠正，真模型路径与规则版同为 12.00s。
> 取舍已落定："有标准答案就自动纠正、无标准答案才只警告"，保证交付物永远正确。

### 补钉：这道闸自己的死角

第一版在"`expected` 算不出来"时直接返回空，出现两个失效态：

| 场景 | 第一版 | 现在 |
|---|---|---|
| 已摄取但**没跑 ② M2** | 报 N 条「多删」—— **误导**，真因不是区间算错，是根本没有事实 | 单条提示：**没有可用的 M2 结果，模型多半是臆测，请先跑 ② 素材理解** |
| `index` 为 None | **0 条警告，完全不核对** —— 和没有这道闸一样 | 同上 |

> **校验器在"算不出来"时保持沉默，等价于没有校验器。** 数据缺失本身就是一种必须报出来的状态，
> 而且要报**真正的原因**（缺数据），而不是伪装成另一种错误（多删）。

顺带修掉一个**假绿测试**：`test_discrepancy_check_handles_malformed_ops` 原只喂畸形 op，
守卫会在"没有可解析 `delete_range`"时提前返回，`all(...)` 作用在**空列表**上恒真 ——
测试永远通过却什么也没检查。现改为混入一条合法 op 并断言具体条数。

**✅ 产品决策已落定（2026-09-24 修复）**：发现不一致且能算出确定性标准答案时**自动改用规则结果**
（`do_plan` 把模型不一致的 `delete_range` 替换为 `expected_semantic_ranges()` 的精确区间，非删除操作保留）。
既保证交付物正确，又通过状态文案把"哪里不对"透明摆出，不是静默替换。仅算不出标准答案时仍只警告。

---

## 八、验收状态

| 项 | 结果 |
|---|---|
| 全量回归 | ✅ **163 passed + 27 subtests**（原 98：+7 幻觉护栏、+7 VAD 段解析、+7 整体判据、+8 按台词删除、+1 UI 路径按台词删除、+5 转写兜底路径、+13 提示词契约与降级回显、**+17 确定性事实核对**） |
| `test_agent` / `test_robust_ops` / `test_understand` / `test_ui` | **43** / 33 / 29 / **28** |
| **真机 LLM 路径** | ✅ 本地 Qwen3-0.6B 跑通完整链路；**提示词→生成→解析→校验→应用**全通；新闸拦下 2 处不一致、0 误报，且有标准答案时**自动纠正为正确成片**（服务已停，不留常驻） |
| **验收脚本复跑** | ✅ `verify_ui` 8/8、`verify_ops` 9/9、`verify_semantics` 5/5、`verify_new_materials_ui` 2/2、`verify_vertical` ✅、`verify_materials` ✅（12.000s / A-V 镜像 / 0 警告） |
| `outputs/` 交付物 | ✅ **11 成片 + 3 验证图逐个 ffprobe 核对**：时长/分辨率/音轨均与文档一致；两条交叉验证样片**仍字节完全相同**（md5 `316d776a…`） |
| UI 模块级处理器全链路 | ✅ 新素材 **2/2 PASS**（素材2 → 12.00s / 1280×720；素材3 → 18.00s / 720×1280） |
| 关键成片 | `样例_删掉bus片段_加字幕.mp4` = **5.00s**；`样例_素材2_删掉三个bus.mp4` 与 `样例_素材2_删掉说公交车的片段.mp4` **均为 12.00s 且字节数完全相同（242798）** |
| 性能 | ✅ 转写分段链路 **5.02s → 1.36s（3.7×）**，端到端 42–51s → 33.96s |
| 文档同步 | ✅ 三份（`交付文档.md` / `M5_VERIFICATION.md` / `交付概览.md`），契约补 ⑧⑨⑩⑪；无残留旧数字 |
| 清理 | ✅ 一次性产物已删；本地模型服务已停（不停留后台常驻） |

---

## 九、沉淀

- 新增 skill：`~/.workbuddy/skills/asr-transcript-hardening/SKILL.md` —— ASR 加固清单：
  症状→根因→修法表、可抄的**单段/整体两级护栏**、**VAD 换时间戳**的完整实现、
  静音阈值的"粒度 vs 幻觉"权衡、**分段后的性能代价与安全兜底**、以及"转写段用于编辑时必须外扩到镜头"。
- 增补 skill：`~/.workbuddy/skills/llm-opchain-robustness/SKILL.md` 新增
  **§2b 提示词自己也要被测试**、**§2c 提示词不得宣称不存在的事实**、**§2d 降级也要回显给用户**，
  反模式补 5 条（含子串断言自欺、整条 prompt 断言误报）。
- 可复用的探测/基准脚本留在 `_verify/`：`probe_vad_segments.py`、`probe_vad_thresholds.py`、
  `bench_transcribe.py`、`bench_batch_asr.py`、`bench_numpy_slice.py`、`verify_speech_delete.py`。
- `ffmpeg-render-pipeline` 已含 5b「字幕坐标系」条目（Bug O 的通用化结论）。

## 十、仍未做（明确标注，非遗漏）

- ~~**句级时间戳**~~ → **重新判定为「当前不需要」**（2026-09-24）：
  段落级/半句级已拿到，逐词需 `punc_model`（数百 MB）。但本项目已确立
  **编辑区间以镜头为单位**，而"按台词删除"一律外扩到镜头边界 ——
  比镜头更细的粒度恰恰是要避免的（"只删半句会留下半个镜头"）。
  因此句级/逐词时间戳在现有需求下没有落点；只有将来要做**亚镜头粒度**的删除时才有必要。
- **云端大模型未在真机上验过**：本机未配置 `DEEPSEEK_API_KEY`（已确认代码里**没有硬编码密钥**，
  UI 只是预填了 SiliconFlow 端点，Key 需用户自己提供）。本轮的"真机验证"用的是
  **本地 Qwen3-0.6B** —— 它已经证明了"真机会答错"，而云端更强的模型理论上错得更少，
  但仍是同一个设计问题。有 Key 后建议复跑一次 `_verify/verify_llm_real.py`
  （改 `LLM_ENDPOINT` / `LLM_MODEL` 即可），看云端是否也残留漏删/多删。
- ~~**不一致是否自动纠正（产品决策待定）**~~ → **已落定并修复（2026-09-24）**：有确定性标准答案时
  `do_plan` 自动纠正为正确区间；无标准答案（无 M2 事实 / 关键词未命中）才只警告。交付物永远正确。
