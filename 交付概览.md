# 交付概览 · 全量功能验收与实测修复（M → R）

> 触发来源：用户问「现在是什么版本？基本功能齐全了吗，你帮我验证测试一下」
> 结论：**核心剪辑闭环齐全**。验收过程中逐层实测，先后查出并修掉 5 类缺陷
> （M 空转 / N 区间过窄 / O 竖屏字幕溢出 / P ASR 幻觉 / Q 提示词契约 / R 模型少删多删）。
> 本文件是**总览**；逐条细节见 `M5_VERIFICATION.md`，权威契约见 `交付文档.md`。
> 最后同步：2026-09-24。

---

## 一、版本状态

| 项 | 值 |
|---|---|
| 里程碑 | **M1–M5 全部闭环**（摄取 → 理解 → 契约 → 智能体 → UI） |
| 自动化测试 | **163 passed + 27 subtests**（原 75：+9 条「操作→成片」层、+2 条效果指令、+7 条语义删除区间护栏、+5 条字幕几何护栏、+7 条 ASR 幻觉护栏、+7 条 VAD 段解析护栏、+7 条整体幻觉判据、+8 条按台词删除、+1 条 UI 路径按台词删除、+13 条**提示词契约**与降级回显、**+17 条确定性事实核对（真机验证驱动的 Bug R，含「无 M2 事实」死角）**） |
| 代码规模 | `ai_video_agent/` **11 个模块 3141 行**；`tests/` **9 个文件 2502 行** |
| 出片能力 | **9 类操作全部真实渲染**（Bug M 补齐后） |
| 运行方式 | 双击 `一键启动界面.bat` → `http://127.0.0.1:7860`（端口被占会自动改用邻近端口，以窗口打印为准） |

---

## 二、验收结论（逐层实测）

| 层 | 结论 | 证据 |
|---|---|---|
| M1 摄取 | ✅ | 15s / 6 场景 / 12 关键帧，耗时 1.4s |
| M2 视觉理解 | ✅ | YOLOv8 检出 `bus(0.5–4.0s)` `bus(10.5–14.0s)` `person` `tie` |
| M2 语音转写 | ✅ | FunASR 转出完整中文句；**VAD 分段后段落级时间戳**（模型进程级缓存。分段开销已从 5.02s 优化到 **1.36s**，见 Bug P） |
| M3 契约 | ✅ | 9 类 handler 全注册；编译器确定性（同输入同输出） |
| M4 智能体 | ✅ | 规则规划器离线输出正确；云端 SiliconFlow 可用；**本地 Qwen3-0.6B 真机跑通**（见 Bug R） |
| M5 UI 接线 | ✅ | ④ 输出顺序由 `APPLY_OUTPUT_KEYS` 单一来源承载，并由 `TestUIOutputContract` 用 `build_app().fns` 取**真实接线**逐位校验（不写死"几个组件"这类会过时的数字） |
| **UI 端到端** | ✅ **8/8** | 8 条指令真实出片，时长逐项对上（13/12/15/8s…） |
| 全量回归 | ✅ | 163 passed + 27 subtests |
| **操作出片覆盖** | ⚠️→✅ | 见下节 |

---

## 三、测出的缺口：4 类操作「空转」（Bug M）

**验证方法**：不读代码，对契约里 9 类操作各构造一条指令跑完整链路，
用 `ffprobe` 时长/流、**抽帧像素**、**PCM 差分**判定「成片到底变了没有」。

| 操作 | 修复前 | 硬证据 |
|---|---|---|
| `add_bgm` | ❌ 被丢弃 | PCM 平均绝对差 = **0.00**（逐样本相同） |
| `move_clip` | ❌ 无变化 | 调序后片段顺序仍是 `[__a, __b]` |
| `transition` | ❌ 无变化 | t=3.0s 像素 `(255,0,0)` → `(255,0,0)` |
| `effect` | ❌ 无变化 | t=1.0s 像素 `(1,0,255)` → `(1,0,255)` |

**根因**：`compiler.py` 从不消费 `clip.effects` / `tl.transitions` / BGM 数据。

**为什么 83 条测试都没拦住**：operation 侧只断言「Timeline 字段变了」，
compiler 侧只断言「命令字符串里有某个片段」—— **没有任何一条断言「操作改变了成片」**。
这是「只测中间态、不测最终产物」的典型盲区。

---

## 四、修复内容

| # | 操作 | 实现 |
|---|---|---|
| 1 | `effect` | 逐片段注入滤镜链：`grayscale→hue=s=0`、`blur→boxblur`、`brightness/contrast/saturation→eq`、`sepia/invert/mirror`、`fade_in/fade_out`；音轨支持 `volume/fade_in/fade_out` |
| 2 | `add_bgm` | BGM 移到独立 `a_bgm` 轨，编译器单独抽出做 `amix=inputs=2:duration=first:normalize=0` **真混音**；支持 volume / fadeIn / fadeOut / adelay |
| 3 | `transition` | 片段边界注入 `fade=t=out` / `fade=t=in`（淡出到黑/白再淡入） |
| 4 | `move_clip` | 改为**重排（reflow）语义**：插到新位置后整轨重排为首尾相接，顺序变化真能进成片 |

**顺带堵住新的静默**：
- `compile()` 返回值新增 **`unsupported`** 列表（未知效果名 / 转场时长过大 / 多视频轨合并），④ 显示成「⚠️ 以下内容不会体现在成片里」
- `_apply_effect` 对效果名做**白名单校验**，不认的当场报错 → 非严格模式下变成可见警告
- 离线规则规划器新增「黑白 / 灰度 / 复古 / 反色 / 镜像 / 模糊 / 淡入 / 淡出 / 亮度 / 对比度 / 饱和度」指令
- LLM 提示词补上可用效果词表与字段说明
- 另补 **`fade_in` / `fade_out`** 画面效果：此前「淡入」会被效果白名单拒绝，而它是最常见的画面效果请求；现在单个片段也能淡入淡出（不必先切两刀）

---

## 五、修复后实测（真实素材 `测试素材.mp4`，15s）

| 操作 | 判据 | 结果 |
|---|---|---|
| 黑白 | 8 个采样点彩度 | 原片最大彩度 **19 → 0**（全帧去色） |
| 淡入淡出转场 | 转场点像素 | `(56,56,59)` → **`(0,0,0)`** |
| 淡入效果 | 片头 0.1s 像素 | `(31,31,33)` → **`(1,1,1)`** |
| 背景音乐 | 音频 RMS | **3202.3 → 3311.3** |
| 片段重排 | 首帧像素 | `(56,56,59)` → **`(16,16,16)`** |

合成素材极限判据：灰度使蓝色 `(1,0,255)` → `(31,28,31)`；重排使首帧由蓝变红；
BGM 使 PCM 平均绝对差达 **1301.97**。

---

## 六、新增的回归护栏

`tests/test_render_ops.py`（9 用例），其中最关键的一条：

```
test_every_op_changes_compiled_output
  —— 对 OPS_V1 里的每一个操作断言「应用后编译产物必须变化」
```

以后新加操作若编译器没消费它，这条测试**直接失败**，同类问题不会再静默存在。

---

## 七、可复跑的验收脚本（`_verify/`）

全部**首次运行自动生成素材**（共用夹具 `_fixture.py`，删掉也能自愈）。2026-09-24 已全量复跑，均通过。

| 脚本 | 作用 | 最近结果 |
|---|---|---|
| `verify_ops.py` | 9 类操作出片矩阵（ffprobe 时长/流） | **9/9** |
| `verify_semantics.py` | 像素 / PCM 差分判定操作是否真生效 | **5/5** |
| `verify_ui.py` | UI 端到端（模块级处理器 + 接线契约） | **8/8** |
| `verify_m2.py` | M2 真实理解（YOLO + FunASR） | ✅ |
| `verify_new_materials_ui.py` | 两段新素材走 UI 处理器全链路 | **2/2** |
| `verify_materials.py` | 素材规格 + 场景 + 静音 + 运动 + 端到端 | ✅ 12.000s / A-V 镜像 / 0 警告 |
| `verify_vertical.py` | 竖屏链路专项（Bug O 的验证物） | ✅ 720×1280 / 18.000s |
| `verify_asr_guard.py` | ASR 幻觉护栏（Bug P） | ✅ 素材2 十一段、素材3 纯音乐为空 |
| `verify_speech_delete.py` | 按台词删除：台词路径 vs 视觉路径交叉验证 + 出片 | ✅ 两路径同为 `[(0,4),(8,12),(16,20)]` |
| `verify_llm_real.py` | **真机 LLM 端到端**（连真实模型 + 按 M2 事实核对） | ✅ 拦下漏删/多删，0 误报 |
| `verify_ui_llm_real.py` | **真模型 + 真 App 路径 + 真出片** | ✅ 真模型 8.00s vs 规则 12.00s（差 4s，见 Bug R） |
| `bench_*.py` / `probe_*.py` | 性能基准与结构探测（诊断用，非验收） | — |
| `demo_*.py` | 出样片到 `outputs/` | — |

---

## 八、给用户的提醒

**Gradio 不会热加载 `.py`** —— 现在开着的界面还是旧逻辑。请：
1. 关掉黑窗口 → 双击 `一键启动界面.bat`
2. 浏览器 `Ctrl+F5` 硬刷新
3. 再走 ①摄取 → ②M2 → ③生成操作 → ④应用并编译

---

## 九、经验沉淀

1. **「操作改了 Timeline」≠「成片变了」** —— 契约层的字段，渲染层可能根本没读。
2. **验收要问「成片哪里变了」，而不是「有没有报错」** —— 测试全绿 ≠ 功能齐全。
3. **只测中间态（字段、命令字符串）不测最终产物，会漏掉整类缺陷。**
4. **`amix` 做 BGM 混音必须带 `normalize=0`**，否则主音频会被按路数压低。
5. **认不出的东西一律显式回显，禁止静默忽略**（与上一轮 Bug J 同一教训，层级更高）。

> 已同步进 `.workbuddy/skills/ffmpeg-render-pipeline` 与 `llm-opchain-robustness`。

---

## 十、后续迭代（2026-09-23 晚 ~ 2026-09-24）：语义删除的粒度与「按台词删除」

M 之后又修复了一串由**新测试素材**和**真机验证**照出来的问题（详见 `M5_VERIFICATION.md`）：

| 编号 | 问题 | 结果 |
|---|---|---|
| **N** | 语义删除只删「关键帧命中跨度」，不删「整个镜头」（残留画面 + 零长度区间） | 区间外扩到镜头。15s 素材：8.00s → **5.00s**；24s 素材 → **12.00s** |
| **O** | 竖屏/小分辨率下字幕横向溢出被裁（ASS 未声明坐标系） | 声明 `PlayResX/PlayResY`。横屏永远看不见这个 bug，**竖屏素材才照出来** |
| **P** | 纯音乐素材被 ASR 幻觉出一整片段英文；且拿不到时间戳 | ① VAD 分段拿回**段落级时间戳**（零新增下载）；② 两级幻觉护栏；③ 分段开销 5.02s → **1.36s**（3.7×） |
| **Q** | **提示词与事实块自相矛盾，且提示词内容零断言** —— 退化分支没自报家门，schema 却宣称"已按镜头聚合"（对模型说假话）；stub 测试只看请求发没发出去 | 退化分支自报家门 + 规则改条件式 + 补**两层**提示词断言（拼装层 / 线上层请求体） |
| **R** | **真机验证**：模型会**少删**（3 个只给 2 个）也会**多删**（多给一段没人要的），而两种错误都是「操作成功、0 警告、合法 JSON」 | 新增**确定性事实核对**闸：答案能用代码算出来就别赌模型。真 App 路径实测：真模型 8.00s vs 规则 12.00s |

### 新增能力：按台词删除

有了带时间戳的转写段后，规划器新增台词检索。**关键设计：台词区间同样外扩到镜头**
（台词段实测粒度可达"半句"，直接删会留下半个镜头）。

素材2（24s，六幕，三个公交镜头）上的**交叉验证**：

| 指令 | 走的路径 | 规划结果 | 成片 |
|---|---|---|---|
| 删掉**说**公交车的片段 | 台词检索 | `[(0,4),(8,12),(16,20)]` | **12.00s**，video=12.000000 / audio=12.000000，0 警告 |
| 删掉**有**公交车的片段 | 视觉检测（YOLO） | `[(0,4),(8,12),(16,20)]` | 同上 |

两条**互相独立**的路径收敛到同一结果 —— 因为素材设计上"公交幕"的旁白正好也提到公交车。

### 一个关键实测：VAD 参数怎么调、以及调细之后的反噬

`max_end_silence_time` 默认 800ms，素材2 六幕旁白只切成 **3 段**（幕间停顿被合并）；
调到 **300ms** 切成 **11 段**，段边界落在 `4150/8150/12140/16160/20160`，
**与幕边界 4/8/12/16/20s 精确吻合**。代价是**纯音乐素材也被切成 6 段**并逐段幻觉
（`'嗯嗯'/'呜啦啦'/'呜呜呜'…`）——单段判据全部漏过，因此新增**整体判据**
（段数 ≥3 + 每段 ≤3 字 + 覆盖 ≥90% 时长 → 整批丢弃）。

**这也验证了一句老话：把参数调好之前，先确认参数到底有没有生效** ——
第一次用 `vad_post_args={...}` 传参，四个阈值给出**完全相同**的结果（静默无效），
查源码才发现 `VADXOptions(**kwargs)` 吃的是**直接关键字**。

---

## 十一、取舍已落定（已修复）+ 两条已知边界

### 取舍：模型结果不对时，只警告 vs 自动纠正？—— **已落定为「自动纠正」，2026-09-24 修复**

真机实测（`verify_ui_llm_real.py`，真模型 + 真 App 路径 + 真出片）：

| 后端 | 成片 | 界面表现 |
|---|---|---|
| 真模型 Qwen3-0.6B | 模型给 4 条 → **自动纠正为 3 条 → 12.00s**（正确） | ③ 里出现核对提示，并标注「已自动改用精确（规则 Planner）结果」 |
| 规则 Planner | **12.00s**（正确） | 正常 |

**修复前差 4.00s（拿到错的 8 秒成片）；修复后 `do_plan` 在规划阶段就把不一致的删除区间自动纠正，
真模型路径与规则版同为 12.00s、差 0.00s、一致 ✅。** 取舍落定："有标准答案就自动纠正、
无标准答案（无 M2 事实 / 关键词未命中）才只警告"，保证交付物永远正确，且"哪里不对"仍透明摆出。

### 已知边界（不修，如实标注）

1. **`delete_range` 对越界区间是静默截断的** —— 24s 素材传 `(0,40)` 会在库层删光且 0 警告；
   只有走 UI 才会被「视频片段全删光」安全闸拦下（❌ 未编译、不产出）。
   **部分越界（删多了但没删光）没有任何警示** —— 这类"模型是不是理解错了指令"本质不可判定，
   只能靠语义事实核对（Bug R 那道闸）覆盖；再加闸只会制造误报。
2. **云端大模型未在真机上验过** —— 本机无 `DEEPSEEK_API_KEY`（已确认代码里**没有硬编码密钥**，
   UI 只是预填 SiliconFlow 端点）。本轮真机验证用的是本地 Qwen3-0.6B。
   有 Key 后改 `LLM_ENDPOINT` / `LLM_MODEL` 复跑 `verify_llm_real.py` 即可。
3. ~~**句级/逐词时间戳**~~ → **重新判定为当前不需要**：编辑区间以镜头为单位，
   比镜头更细的粒度恰恰是要避免的（"只删半句会留下半个镜头"）。
