# M5 Gradio UI · 验证报告

**日期**：2026-09-18 → 2026-09-23 · **里程碑**：M1→M4 契约 + M5 演示界面闭环 + 本地 Qwen 实跑接入 + 混合后端定稿 + **实跑 bug 修复（A–L）+ 接通真实出片（ffmpeg 渲染 MP4）+ A/V 对齐铁律**

## 交付物

`ai_video_agent/ui.py`（Gradio `gr.Blocks` 单页）+ `ai_video_agent/render.py`（真实出片）+ `tests/test_ui.py`（20 用例）+ `tests/test_robust_ops.py`（29 用例）+ `tests/test_render.py`（3 用例）+ `tests/test_render_ops.py`（9 用例，**「操作→成片」层**）+ `E:\AI_models\Qwen_Model\serve_qwen3.py` + `start_qwen.bat`。

| 组件 | 职责 |
|------|------|
| `build_app()` | 装配单页界面，返回 `gr.Blocks` |
| `do_ingest` | 上传视频 → ffprobe 元数据 + 镜头检测 + 初始 Timeline（`gr.State`） |
| `do_understand` | 可选 M2：YOLOv8 视觉段 + FunASR 转写（依赖缺失优雅降级，不崩；模型进程级缓存，重复点击 4.2s vs 首次 44.3s） |
| `do_plan` | 自然语言 → `[{type,payload}]`（`EditingAgent.plan`，四模式：仅云端（默认）/混合/规则/仅本地） |
| `do_apply_compile` | `operations.apply_operations`（派生 id 解析 + 失败只跳过 + 配对音频同步）→ `compiler.compile()` 生成指令 → `render.render()` **真跑 ffmpeg 出 MP4**（界面 `gr.Video` 可预览/下载）+ 展示 FFmpeg 命令 + 字幕 ASS；视频被删光时安全闸拦截 |
| `render.render()` / `resolve_ffmpeg()` | 定位 ffmpeg（项目自带 `tools/ffmpeg/bin/`）并按次时间戳目录渲染成片，返回成片路径；失败优雅返回原因 |
| `do_reset` | 清空全部状态与展示 |
| `OpenAICompatibleProvider` | 真 LLM 热插拔点（stdlib `urllib`，零新依赖；填 endpoint+key 即切 `JsonPlanner`） |
| `HybridProvider` | **云端优先(SiliconFlow/DeepSeek-V4-Flash)+本地兜底(Qwen3-0.6B)**：本地输出不可解析时自动兜回云端 |

## 验证结果

- **单元/冒烟（离线、无需服务）**：`tests/test_ui.py` **20 passed**
  - `build_app` 返回 `gr.Blocks`（构造期校验组件接线）；
  - 内存构造 `FootageIndex/Timeline` 直调 `do_plan`+`do_apply_compile`：切一刀 / 语义检索「删掉有bus的片段」(1 条 `delete_range`) 整链通过；未摄取安全降级；
  - 真 LLM stub 端到端 `TestLLMEndToEnd` ×2（JsonPlanner 解析 + 鉴权头 + 两种 endpoint 填法）；
  - `TestHybrid` ×2（本地优先+云端兜底）+ `TestHybridCloudPreferred` ×3（云端优先+本地兜底路由）+ `TestHybridValidator` ×5（`_is_valid_ops` 校验）；
  - **`TestUIOutputContract` ×3**：输出顺序契约（按接线组件类型逐位校验 / 按内容校验归位：视频格不得是 JSON、命令格才是 ffmpeg / 各处理函数输出数 == 返回标注元数）。
- **本地 Qwen 真实接入（2026-09-20）**：`E:\AI_models\Qwen_Model\Qwen3-0.6B` 经 `serve_qwen3.py`（GPU/cu132 venv）监听 `http://127.0.0.1:8001/v1`。
  - `curl /health` → cuda True、模型 lazy-load 正常；`/v1/chat/completions` 对"去掉前2秒"产出 **合法 JSON** `[{"type":"trim","payload":{"targetClipId":"src_video","in":2.0,"out":10.0}}]`（JSON 可解析）。
  - ⚠️ **质量实测偏弱**：对"去掉前2秒"偶发错答为 `delete_clip` 或空数组、`"在第2秒切一刀"` 漏 `at` 字段——0.6B 小模型不足以稳定遵循 schema，故定为**仅离线/断网兜底**，不做主规划。
- **云端兜底实测可用**：用户 Key 真实网关 **SiliconFlow**（`https://api.siliconflow.cn/v1`，模型 `deepseek-ai/DeepSeek-V4-Flash`），真实 NL→操作整链正确。
- **真实视频端到端**：用静态 ffmpeg 生成 6s/320×240 测试片，跑通
  `do_ingest` → `do_plan("去掉前2秒")` 产出 trim → `do_apply_compile` 产出
  `ffmpeg -i sample.mp4 -filter_complex [0:v]trim=start=2.0:end=6.0,...[vout] -map [vout] ...`。
- **起服验证**：`launch(prevent_thread_lock=True)` 绑 `127.0.0.1:7869/7860/7861` 正常，`close()` 正常。
- **真实出片实测（2026-09-23）**：`测试素材.mp4`（15s，1280×720，含音轨）跑「去掉前2秒」→ 真正产出 `outputs/<ts>/output.mp4`；ffprobe 校验 **video 流 13.000s / audio 流 13.000s / 总时长 13.000s**（A/V 完全对齐）。`tests/test_render.py` 另用临时生成片源验证「渲染 → ffprobe 时长 > 0」。
- **A/V + 多区间删除实测（2026-09-23 修复后）**：跑「删掉有bus的片段 + 加字幕"开场"」→ 成片 **8.00s**；`src_video` 与 `src_audio` 各 3 段 `(0,0.5)(4,10.5)(14,15)` **逐段镜像**；视频链 `concat=n=3:v=1:a=0`、音频链 `concat=n=3:v=0:a=1`；音频溯源曲线在 0.50s 处由源 0.5s 跳到源 4.00s（证明音频跟着剪辑走）；字幕 1 条不重影。新增 `TestAvSyncOnVideoEdit` ×7 + `TestCompiledAudioGraph` ×1 + `TestSubtitleDedupe` ×2 锁死。
- **全量回归**：`pytest tests/ -q` → **163 passed + 27 subtests** = test_agent 43 + test_contract 14 + test_ingest 3 + test_pipeline 1 + test_understand 29 + test_render 3 + **test_render_ops 9** + test_robust_ops 33 + test_ui 28。

## 实跑 bug 修复（2026-09-23，用户界面截图反馈）

用户反馈：① 点「② 跑视觉理解」跑不了；② 点「④ 应用并编译」报 `KeyError: 'clip not found: src_video'`；③ 重启时 `OSError: Cannot find empty port in range: 7860-7860`；④「结果只返回代码，不是该返回一个视频吗」；⑤「④ 一按下去，右侧整块 5 格全变成『错误』卡」。均已修复。

**Bug A：编译失败——操作链引用被 cut 拆掉的旧 id**
- 根因：`cut src_video` 后原 id 消失（变 `src_video__a`/`__b`），LLM 后续 op 仍写 `src_video` → 裸 `KeyError` 让整批操作全废。
- 修复：`operations.apply_operations()` 派生 id 别名表，按 `at`/`in`/`out` 时间提示自动解析（`src_video` @8.0s → `src_video__b`）；歧义场景明确报错列出候选；UI 走非严格模式（单条失败只跳过）+ 视频删光安全闸。

**Bug B：LLM 输出偶发不可解析（`Extra data: line 1 column 3`）**
- 修复：`agent._extract_json_array()` 容错提取（围栏/前缀说明/多段 JSON）+ 解析失败自动重试一次 + 报错回显两次原始输出；新增 `_validate_ops()` 拦截契约外操作名。

**Bug C：规划器缺真实上下文（A 的上游根因）**
- 旧提示词只给 `timeline.dumps()`，模型不知道 bus 在哪几秒 → 实测直接产出 `delete_clip src_video` + `delete_clip src_audio`（把整条视频删光）。
- 修复：注入「可用 clip id 清单」+「M2 事实（镜头/物体区间/台词）」+ cut 派生命名规则 + **单/多区间两组 worked example**。多区间删除从"3 次里推歪" → **连测 3 次全对** `(0,0.5)(4,10.5)(14,15)`。

**Bug D：M2 点按体验**
- 排查结论：`do_understand` 本体无 bug（`gradio_client` 实测 18–26s 正常返回）。
- 真实问题：首次要加载 YOLO+Paraformer 权重（44.3s，含一次性下载）且零反馈；FunASR 默认联网查版本。
- 修复：模型缓存（二次 4.2s）、`disable_update=True`、耗时回显、`demo.queue()`+`show_progress="full"`、未摄取提示改为"请先点①"。

**Bug E：二次启动报 `OSError: Cannot find empty port in range: 7860-7860`**
- 现象：关掉界面窗口后再双击 `一键启动界面.bat`，新窗口崩在 `launch()`。
- 根因：关窗后 `python -m ai_video_agent.ui` 进程未退出，仍占着 7860（实测残留进程 1.5 GB，已加载模型）。
- 修复（双保险）：① `.bat` 启动前用 PowerShell 按命令行精确匹配 `*ai_video_agent.ui*` 清理本项目残留 UI 进程（不影响 photoshop-mcp-server 等其它 python）；② `ui.py` 新增 `_pick_free_port()`，7860 被占自动顺延 7861–7879（`GRADIO_SERVER_PORT` 可覆盖）。
- 实测：7860 被占时返回 7861；清理后 7860 释放，新实例 `GET /` 返回 **HTTP 200**，5 端点全就绪。

**Bug F：④ 只返回命令字符串、不产出视频（用户反馈「返回这些代码很不可观」）**
- 根因：`compiler.compile()` 是纯函数，只生成 ffmpeg 命令却从不执行；UI 也无 `gr.Video` 输出。
- 修复：① `compile()` 新增结构化 `argv`（`command` 字符串不变，兼容确定性测试）；② 新增 `render.py`（定位项目自带 ffmpeg + 按次时间戳目录渲染）；③ `do_apply_compile` 返回成片路径 + 界面新增 `gr.Video`「④ 成片预览/下载」；④ 渲染失败不影响编译结果展示。
- 实测：真实产出 `outputs/<ts>/output.mp4`。

**Bug G：真跑 ffmpeg 后暴露的编译/操作链缺陷（2 处）**
- **G1 单音频滤镜链拼接错误**：单音频分支产出 `...[ca0][ca0]anull[aout]` → ffmpeg 报 `Trailing garbage after a filter`。修复：单音频直接 `af = aparts[0]`、`amap = alabels[0]`。
- **G2 trim 只裁视频不裁配对音频**：`_apply_trim` 增加「同源、同初始时窗、不同轨」clip 的**配对同步**（连 `sourceIn/sourceOut` 一起平移）；ffmpeg 命令加 `-shortest` 兜底。
- 实测：「去掉前2秒」→ video/audio 流均 13.000s，A/V 对齐。

**Bug H：字幕显示成 `\an2开场`（ASS 覆盖标签缺 `{}`）**
- 根因：`build_ass_content()` 拼成 `\an2开场`，而 ASS 覆盖标签必须用 `{}` 包裹。
- 修复：改为 `{\an2}开场`；`test_contract.py` 断言加严为 `assertIn("{\\an2}你好", ass)`。
- 实测：抽帧确认字幕正确显示为「开场」并底部居中。

**Bug I：④ 一按下去右侧 5 格全变「错误」卡（Gradio 位置契约错位）**
- 现象：点④后 ④ 的整块输出（状态/成片/JSON/命令/字幕）全渲染成"错误"卡，卡里没有任何原因文字；而后端其实**成功**了（`outputs/<ts>/output.mp4` 真实存在）。
- 根因：`click(outputs=[...])` 是**位置契约**。加 `video_out` 时插在 outputs 列表第 3 位，`video_path` 却追加在返回元组末尾 → 整批后移一格，`gr.Video` 拿到一段 Timeline JSON 当文件路径。服务端后处理该"路径"时抛异常，而 **Gradio 里一个输出的后处理失败会让该依赖的全部输出显示为错误卡**（`gradio_client` 实测收到 `AppError`）—— 这正是"5 张卡"的来源。
- 修复：① `ui.py` 新增唯一顺序常量 `APPLY_OUTPUT_KEYS`，`do_apply_compile` 用内部 `out(**kw)` 按它摊平成元组，手写元组字面量被消除；② 输出列表旁加注释指向该常量；③ `launch(..., show_error=True)`，以后出错会直接显示真实异常而不是一张无信息的卡（对外演示可改回 `False`）。
- 防回归：新增 `TestUIOutputContract` ×3，用 `build_app().fns` 取**真实接线**组件按类型逐位校验 + 按内容校验归位（视频格不得以 `{` 开头、第 4 格必须是合法 JSON、第 5 格必须含 `ffmpeg`）。**已反向验证**：把返回临时改回旧写法，该测试当场失败并打印 `video_out 收到了 JSON：'{\n  "metadata"...'`。
- 实测：真服务 + `gradio_client` 全链路，视频格拿到真实文件路径（文件存在、520KB）、JSON/命令/字幕各归其位，成片 13.000s / A/V 对齐；加字幕那一路 `ass` 格正确。

**Bug J：指令「删掉有bus的片段」只生效一半（多区间删除被静默跳过）**
- 现象（用户带着成片反馈）：成片里 bus 还在；用户感觉"输出和提示词有差别"。
- 逆向取证：`ffprobe` 时长 11.52s（源 15s）；`silencedetect` 静音位置与源**逐毫秒一致** → 音频没剪；逐帧静止段 `[(8,111),(134,110),(258,24)]` vs 源 `[(8,111),(134,110),(258,111)]` → **成片 = 源 `[0,10.5]++[14,15]`**，即**只删掉了第二段 bus（10.5–14），第一段（0.5–4）原样保留**。
  - ⚠️ 差点误判：源素材本身是 **3 张静态图各停 ~4.4s** 的幻灯片，"相邻帧完全相同"是源的特征，不是渲染冻结。
- 根因：M2 事实有**两个** bus 区间。旧提示词要模型「切两刀 + 删中间」并自己推演派生 id；第一组三步后 id 全变，第二组引用失效被**静默跳过**（`cut at=4.0 必须严格落在 (14.0,15.0)`、`src_video__b__a 不存在`）→ 只删了一段。
- 修复：新增**免 id 的范围型操作 `delete_range: {start, end}`**（素材绝对秒数）；执行器自己拆两刀删中间；提示词与 `RuleBasedPlanner` 同步改为每区间一条 `delete_range`；命中不到任何 clip 时抛错告警而非静默成功。
- 实测：同指令 → 成片 **8.00s**，视频/音频各 3 段 `(0,0.5)(4,10.5)(14,15)` 镜像。

**Bug K：音频不跟随视频剪辑（A/V 失步）**
- 用户提问：「删除前两秒，音频不也会被删除掉的吗」——旧版本**不会**。
- 根因：`src_audio` 是独立 clip，只有 `trim` 做了配对同步，`cut`/`delete_clip` 没管音频；`-shortest` 只按视频长度**截断**音频，不会让它跟着剪辑走。实测成片视频 `源[0,10.5]++源[14,15]` 而音频是 `源[0,11.52]` 连续原声 → **10.5s 起音画错位**。
- 修复（确立**「视频是主轨、音频跟随」**铁律）：① `cut` 在同一素材时刻切开配对轨；② `delete_clip`/`delete_range` 按素材区间从配对轨摘掉同一段（反向不成立）；③ `_apply_trim` 配对判据改为**素材区间相等**（cut 之后仍能配对）；④ 编译器多段音频由 `amix`（并行混音）改为 **`concat=n=K:v=0:a=1`**（顺序拼接）。
- 实测：`src_video__a/b__a/b__b` 与 `src_audio__a/b__a/b__b` 素材区间完全一致；音频溯源在 **0.50s 处由源 0.5s 跳到源 4.00s**（旧版本是直线）。
- 洞察：`ffprobe` 显示两条流时长相等**不能证明 A/V 同步**（`-shortest` 会把错位成片伪装成正常）——需做**内容溯源**。

**Bug L：字幕重影（同一条字幕画两遍）**
- 现象：`subs.ass` 里有两条完全相同的 `Dialogue`，成片里文字明显加粗/重影。
- 根因：LLM 把同一条 `add_subtitle` 输出两遍，而 `_apply_subtitle` 按 v1 契约允许重叠。
- 修复：`apply_operations` 对 `add_subtitle` 去重（文本 + 时间窗 + 位置 + 轨道全同才去重）并记警告。
- 实测：字幕 1 条，抽帧确认单层渲染。

**Bug M：4 类操作「改得了时间轴、改不了成片」（空转 / 静默失效）**
- 发现方式：用户问「基本功能齐全了吗」→ 不读代码，直接做**出片矩阵验证**：对契约里 9 类操作各构造一条指令跑完整链路，用 ffprobe / 抽帧像素 / PCM 差分判定「成片到底变了没有」。
- 现象：`add_bgm` / `move_clip` / `transition` / `effect` 四类都能成功改 Timeline、界面显示「✅ 已应用并编译」，**成片却与不加一模一样**。
- 硬证据：加 BGM 前后 **PCM 平均绝对差 = 0.00**（逐样本相同）；`effect(grayscale)` 后 t=1.0s 像素仍 `(1,0,255)`；`transition(fade)` 后边界仍 `(255,0,0)`；`move_clip` 调序后顺序仍 `[__a, __b]`。
- 根因：`compiler.py` **从不消费** `clip.effects` / `tl.transitions` / BGM 数据。
- 为何 75 用例全绿没拦住：operation 侧只断言「Timeline 字段变了」，compiler 侧只断言「命令串里有某个片段」，**没有任何一条断言「操作改变了成片」**。
- 修复：effect→逐片段滤镜链（含画面 `fade_in`/`fade_out`：单个片段也能淡入淡出）；add_bgm→独立 `a_bgm` 轨 + `amix=inputs=2:duration=first:normalize=0` 真混音；transition→边界 `fade=t=out/in`；move_clip→整轨**重排**语义。另加 `compile()["unsupported"]` 回显 + `_apply_effect` 效果名白名单校验（不认的当场报错转可见警告）。
- 实测（真实素材 15s）：黑白 8 点彩度 **19→0**；转场点像素 `(56,56,59)`→**`(0,0,0)`**；BGM 音频 RMS **3202.3→3311.3**；重排首帧 `(56,56,59)`→**`(16,16,16)`**。

**Bug N：语义删除只删「关键帧命中跨度」而非「整个镜头」**
- 发现方式：新增测试素材（3 个公交镜头）后跑同一句 `删掉有bus的片段`，规划出 `(2.0, 2.0)` —— **零长度区间**，严格模式报错、UI 非严格模式静默跳过。
- 根因：`detect_objects` 按**关键帧**粒度合并同标签命中，而不是按**镜头**；单关键帧命中的镜头 → `(t, t)`；多关键帧命中 → 区间仍盖不满整镜。
- 回溯：解释了 Bug J 成片为何还残留 `0–0.5s` 与 `4–5s` 的公交画面 —— "bus 没删干净"到这里才真正解决。
- 修复：`understand.py` 新增 `_scene_index_at` / `_label_ranges_from_times`，把命中外扩到镜头边界（边界时刻归下一镜），相邻命中镜头合并。
- 实测：`测试素材.mp4` 15s → **5.00s**（旧版 8.00s，残留 3s 公交）；`测试素材2_多目标.mp4` 24s → **12.00s**，A/V 逐段镜像、0 警告。
- 护栏：`tests/test_agent.py::TestSemanticRemoveCoversWholeShot` ×7。

## 测试素材套件（3 段，互补覆盖）

| 素材 | 规格 | 结构 | 专测什么 |
|---|---|---|---|
| `测试素材.mp4` | 15s / 1280×720 / 25fps + 中文旁白 | 3 幕：公交→人物→公交（公交 0–5、10–15s） | 基线；「删掉有bus的片段」双区间、A/V 溯源 |
| `测试素材2_多目标.mp4` | 24s / 1280×720 / 25fps + 逐幕对齐中文旁白 | 6 幕交替（**3 个公交镜头** 0–4 / 8–12 / 16–20s） | **多区间删除**（3 段）、六幕跳切、多目标 YOLO |
| `测试素材3_竖屏音乐.mp4` | 20s / **720×1280 竖屏** / 25fps + 合成音乐 | 5 幕 + zoompan 真实推镜 | 竖屏缩放/字幕排版、**ASR 无语音降级**、无静音段的连续音频溯源、运动画面 |

实测校验（`_verify/verify_materials.py`，全部硬测量）：

| 素材 | 场景切点 | 静音段 | 幕内帧差 | YOLO 类别 |
|---|---|---|---|---|
| 素材2 | 5 个 → `[4,8,12,16,20]`（正好 6 幕） | 13 段（旁白+逗号停顿） | `0.0`（静态图，符合设计） | person / bus / tie |
| 素材3 | 4 个 → `[4,8,12,16]`（正好 5 幕） | **0 段**（连续音乐） | `2.63/0.87`（推镜真在动） | person / bus / tie / tennis racket |

> 生成脚本 `scripts/make_more_clips.py`（可重跑）。**幕间必须硬切**：加了逐幕淡入淡出后，
> 1Hz 采样的场景检测会把 1 个切点算成 2 个（黑帧 vs 前幕 / 后幕各一次），6 幕素材被判成 12 个场景，
> 且落在黑帧上的关键帧喂给 YOLO 什么都检不出。这是实测踩出来的，别再犯。

**Bug O：竖屏/小分辨率下字幕横向溢出被裁（ASS 未声明坐标系）**
- 发现方式：新素材3（720×1280）加字幕后**抽帧肉眼检查** —— 6 个汉字撑满 720px、两端各裁掉约一个字；横屏同一代码看着正常。
- 根因：ASS 没写 `PlayResX/PlayResY`，libass 按 384×288 解释坐标 → 声明的 `Fontsize: 36` 实为"画面高度的 12.5%"，
  有效字号只跟高度走；`WrapStyle: 2` 又不折行。Style 的 `Format` 还少了 3 个字段并重复声明 `Outline`/`Shadow`。
- 修复：声明 `PlayResX/PlayResY`、`WrapStyle: 0`、`ScaledBorderAndShadow: yes`；改标准 V4+ 23 字段 Format；
  字号 `min(H×4.8%, W×5.5%)`，描边/阴影/边距同比例缩放。
- 实测：竖屏 6 字完整落在画面内（`outputs/验证_竖屏字幕.png`）；横屏字号收敛到合理值。带字幕样片已全部重出。
- 护栏：`tests/test_contract.py::TestSubtitleFitsFrame` ×5。

**Bug P：纯音乐素材被 FunASR 幻觉出一整片段英文（污染事实块与界面）**
- 发现方式：新素材3（纯音乐、无任何语音）跑 M2 转写 → 返回 `0.00–20.00s "the"`，一个横跨全片、只有一个英文单词的"台词"。
- 根因：FunASR 对无语音音频幻觉出短英文词；退化分支又把 `start/end` 取成 `0.0 / metadata["duration"]`，把幻觉放大成"整片都在说 the"。
- 顺带查清（实测，别再重复试）：① 当前配置**拿不到句子级时间戳** —— `generate()` 返回体只有 `{'key','text'}`，无 `sentence_info`，`sentence_timestamp=True` 不生效；实测同一条旁白只能得到整片一条且**幕序错乱**。句级断句需 `punc_model`（数百 MB），暂不引入。② `vad_model="fsmn-vad"` **挂在 AutoModel 里**无效：仍是 1 段、仍无 `timestamp`、**音乐幻觉依旧**。
- 修复（三段）：
  1. **幻觉护栏（单段）** `understand._looks_like_hallucination()` —— 仅当同时满足「覆盖 ≥90% 时长」+「不含汉字」+「词数 ≤ 2」才丢弃；带真实断句的段一律保留。
  2. **VAD 分段转写**（`_get_vad` / `_vad_spans` / `_slice_wav`）—— 关键认知：**单独实例化 VAD**（`AutoModel(model="fsmn-vad")`）才拿得到原始段边界 `[[起ms,止ms],…]`；挂进 `AutoModel` 会被"切完又拼回一条"而丢掉边界。拿到边界后**逐段切音频、逐段转写**。
  3. **静音阈值调到 300ms**（默认 800）+ **整体幻觉判据**（`_looks_like_silent_footage`）。这两件事必须一起做，理由见下。
- 实测（素材2 六幕旁白）：粒度从「整片一条 + 幕序错乱」→ 默认阈值 800ms 的 **3 段** → **300ms 的 11 段**，且段边界落在 `4150/8150/12140/16160/20160`，**与幕边界 4/8/12/16/20s 精确吻合**（差值即旁白起始的 0.25s 延时）：
  `[0.08-1.28] 第一幕` / `[1.28-3.35] 画面里有一辆公交车` / `[4.15-5.27] 第二幕` / …
- **调细阈值的反噬（重要）**：同一改动让**纯音乐素材从 1 段变成 6 段**，逐段幻觉出 `'嗯嗯'/'呜啦啦'/'呜呜呜'/'嗯'…`（素材3）。此时**单段判据全部漏过** —— 每段只覆盖 16% 时长（`covers_all` 不成立），文本又含汉字（`has_cjk` 反而成了"像真台词"的证据）。→ 新增**整体判据** `_looks_like_silent_footage`：段数 ≥3 + 每段 ≤3 字 + 区间并集覆盖 ≥90% 时长 → 整批丢弃。实测素材3 恢复为**空**。
- 一个反直觉的实测：**VAD 会把纯音乐整段判成语音**（素材3 默认阈值 → `[[0, 19980]]` 覆盖全片），所以"有 VAD"≠"没幻觉"—— 护栏必须保留。
- **参数怎么传（踩过一次）**：VAD 参数是**直接作为关键字**传给 `AutoModel` 的（`VADXOptions(**kwargs)` 消费）。签名里那个 `vad_post_args` **从未被使用** —— 第一次用 `vad_post_args={"max_end_silence_time": 300}` 传参，四个不同阈值给出**完全相同**的结果（静默无效），差点得出"阈值不影响分段"的错误结论。
- 取舍：① 素材里若真的只有一句极短英文台词（如 `"Go!"`）或整片只有 1–3 字短句，会被丢弃；② 转写段时间戳为**段落级**。影响可控（转写段只进事实块与展示、**不参与删除决策**，实际删除走镜头区间）。
- 实测：素材2 → **11 段**且与幕边界对齐；素材3 → **转写段为空**。复跑 `_verify/verify_asr_guard.py`。
- 护栏：`TestAsrHallucinationGuard` ×7 + `TestVadSpans` ×7 + `TestSilentFootageGuard` ×7。

**性能：分段带来的代价，以及怎么把它压回去**
- 分段把 1 次转写变成 N 次（素材2 → 11 次）。A/B 实测：整片一次 **1.38s**，VAD 分段 **5.03s**（3.64×）。
- 但拆开看 —— **5.03s 里转写只占 1.72s，剩下 3.30s 全花在 11 次 ffmpeg 切音频的进程开销上**。
  **优化重点根本不在模型，在切段方式**。（这个结论只有把两段分开计时才看得到。）
- 解法：**一次读 wav 成 numpy → 按 VAD 段切片 → 批量 `generate(input=[...])`**。
  实测 **5.02s → 1.36s（3.7×）**，且**文本逐条完全一致**。
  批量调用本身也小赚一点（11 段逐次 1.72s → 一次 1.39s，省掉重复的前后处理）。
- **"一致"必须显式验证**：数组的 dtype / 取值范围不对时，模型**不一定报错**，可能静默转出垃圾。
  所以验证脚本是**逐条比对文本 + 比对条数**，不是只看跑不跑得通。
- 两级兜底（都不能少）：① soundfile 不可用 / 采样率不符 / 切片后段数对不上 → 退回 ffmpeg 切段；
  ② **批量返回条数 ≠ 输入条数 → 退回逐条** —— 否则时间戳会错位到别的段，这比慢一点严重得多。
- 护栏：`tests/test_understand.py::TestAsrSegmentTexts` ×5（mock 掉模型与 ffmpeg，专测这两条兜底路径）。
- 复跑：`_verify/bench_transcribe.py`（分段代价）、`_verify/bench_batch_asr.py`（批量 vs 逐条）、
  `_verify/bench_numpy_slice.py`（内存切片 vs ffmpeg 切段）。

**新增能力：按台词删除（`_speech_ranges` / 规划器分支 a2）**
- 有了带时间戳的转写段后，规划器支持「删掉**说** X 的片段」这类指令。
- **关键设计：台词区间同样外扩到镜头**。台词段粒度可到"半句"（`[8.15,9.34] "第三幕"`），
  直接拿它去 delete_range 会只删半句、留下半个镜头 —— 与 Bug N 是同一条铁律。
  `_speech_ranges` 把它扩成覆盖的镜头 `(8.0,12.0)`。
- 事实块同步改造：台词**按镜头聚合**后再交给 LLM（`镜头 8.00-12.00s: "第三幕" / "又出现了一辆公交车"`），
  模型拿到的就是**可直接使用的删除区间**，不需要自己推演外扩（推演极易出错）。
- 视觉优先、台词兜底：指令形如「删掉**有**X的片段」先查视觉标签，视觉无匹配再回退台词检索
  （解决"用户说中文『公交车』、视觉标签却是英文 bus"的错位）。
- **交叉验证**（素材2，24s）：「删掉**说**公交车的片段」（台词路径）与「删掉**有**公交车的片段」
  （视觉路径）给出**完全相同**的 `[(0,4),(8,12),(16,20)]`，成片均 **12.00s**、
  video=12.000000 / audio=12.000000、**0 警告**。复跑 `_verify/verify_speech_delete.py`。
- 护栏：`tests/test_agent.py::TestDeleteBySpeech` ×14。

**Bug Q（2026-09-24 补修）：提示词与事实块自相矛盾 + 提示词内容零断言**
- **发现方式**：回头审提示词契约，两件事同时成立 ——
  ① `facts_block` **拿不到镜头信息**时会退化成逐条列台词（没有「镜头 X-Y」），
  而 schema 规则 3b **无条件**写着"事实里台词已**按镜头聚合**，用「镜头 X-Y」就是删除区间"；
  ② 提示词内容**没有任何断言**（stub 测试只看 model 名 / messages 非空 / 鉴权头），
  所以事实块或规则被重构改坏时**所有测试照样绿**，LLM 路径已悄悄退化。
- **后果**：提示词在对模型**说假话** → 模型去找不存在的「镜头 X-Y」，或违反 3b 改用台词小时间戳
  → 又回到"只删半句、留下半个镜头"，而且**不报错**。
- **修复**：① `facts_block` 退化分支**自报家门**（`台词（**未按镜头聚合：…**）`），正常分支保持
  `台词（**按镜头聚合：…**）`，两条路径首个标识不同；② 规则 3b 改**条件式**（先看标识再选数据）
  并补一个按台词删除的 worked example；③ 补**两层**提示词契约测试（拼装层断言 `build_prompt()`、
  线上层让 stub 存下**服务端实际收到的请求体**再断言）。
- **顺带补的降级回显**：规则路径没有模型兜底 —— 没有镜头信息时 `_speech_ranges` 只能给 1–2s 半句区间。
  新增 `speech_delete_degrades_to_raw_segments(cmd, index)` 判定，`do_plan` 状态里显式提示，**不许静默降级**；
  判定正则与规划器**共用** `_SPEECH_DELETE_RE`，避免两处各写一份日后分家。
- **测试写法两个坑（已写进注释）**：① `"未按镜头聚合"` **包含** `"按镜头聚合"`，只 `assertIn("按镜头聚合")`
  会在退化场景**假通过** → 用带前缀标识 + 反向 `assertNotIn`；② 对**整条**提示词 `assertNotIn` 必然误报
  （schema 的【示例】区天然含同形字符串）→ 先按标记**切出事实段**再断言。
- 护栏：`tests/test_robust_ops.py::TestSpeechPromptContract` ×4（含交叉断言，防提示词与事实块各自演化分家）
  + `tests/test_agent.py` 事实块退化/降级判定 ×6 + `tests/test_ui.py` 线上层契约与降级回显 ×3。
- **实测复跑**：`_verify/verify_speech_delete.py` 与修复前**完全一致**（11 段转写、两条路径同为
  `[(0,4),(8,12),(16,20)]`、成片 12.00s、0 警告）。

**Bug R（2026-09-24）：真机跑通 LLM 路径 → 模型少删/多删且不报错（→ 新增确定性事实核对闸）**
- **怎么发现**：第一次用**真实模型**（本地 Qwen3-0.6B @ 8001）跑通 LLM 规划路径
  （`_verify/verify_llm_real.py`）。此前这条路径只有 stub 验证过，而 stub 自己造返回内容，
  永远测不出"模型到底照不照做"。
- **实测结果**（`temperature=0`，两次跑出完全相同的输出 → 稳定偏差而非偶发）：

  | 指令 | 模型输出 | 后果 |
  |---|---|---|
  | 删掉**说**公交车的片段 | 只 2 条 `(0,4)` `(8,12)` | **少删**：16–20s 那幕还在成片里 |
  | 删掉**有**公交车的片段 | 4 条，多一条 `(20,24)` | **多删**：删了用户没要的第 6 幕（8s 而非 12s） |
  | 去掉前2秒 | `(trim, in=2.0)` | ✅ 正确 |

  **两次错误都是操作成功、0 警告** —— 容错解析/重试/别名表全救不了，因为模型给的是**合法 JSON**。
- **好消息**：模型没用台词小时间戳 → 规则 3b + 聚合事实块在真机上确实生效；
  输出总带 ` ```json ` 围栏 → `_extract_json_array` 的围栏剥离被真机验证覆盖到。
- **修复**：语义删除本质上是**检索**（答案已在 YOLO/ASR 事实里），所以可以**确定性核对**而不必赌模型。
  新增 `expected_semantic_ranges()` + `semantic_delete_discrepancies()`，`do_plan` 状态里
  明确报出「漏删 / 多删」；**能算出确定性标准答案时自动纠正**为正确区间（`do_plan` 把模型不一致的
  `delete_range` 替换为 `expected_semantic_ranges()` 的精确结果，非删除操作保留），无标准答案才只警告。
- **实测 ②（真模型 + 真 App 路径 + 真出片，`_verify/verify_ui_llm_real.py`）**：
  `do_plan` → `do_apply_compile` → 渲染 → ffprobe —— 真模型原给 4 条（含多出的 20-24），
  **`do_plan` 自动纠正为 3 条 → 成片 12.00s（正确**，③ 状态里核对提示出现并标注「已自动改用精确结果），
  规则 Planner 成片 **12.00s**（正确），**差 0.00s、一致 ✅**（修复前差 4.00s、拿到错的 8 秒成片）。
- **补钉（同一轮追加）**：第一版在"期望值算不出来"时保持沉默 —— `index=None` 时**完全不核对**，
  空索引时报成「多删」（误导）。现在这两种都给出同一条准确提示：
  **没有可用的 M2 结果，模型多半是臆测，请先跑 ② 素材理解**。
  教训：**校验器在"算不出来"时沉默，等价于没有校验器**；数据缺失本身就是要报的状态。
  顺带修掉一个假绿测试（守卫提前返回导致 `all(...)` 作用在空列表上恒真）。

**验收脚本自包含性（2026-09-24 复跑时发现）**
- `verify_semantics.py` 单独复跑会崩：它直接探测 `_verify/clip6.mp4`，而这份素材**只有
  `verify_ops.py` 的 `ensure_fixture()` 会生成** —— 只有"先跑过 verify_ops"才侥幸能过。
  验收脚本的价值就在随时可复跑，所以这个依赖顺序的隐式契约必须去掉。
- 修法：把生成逻辑抽到共用夹具 `_verify/_fixture.py`（6s / 640x360 / 前 3s 蓝 + 后 3s 红 + 440Hz 音轨），
  两个脚本都改成调用它。**实测：删掉 clip6.mp4 后单独跑 verify_semantics → 自己生成并 5/5 通过。**
- 全量复跑（改动只动了 agent/ui，但验收脚本断言状态文案，值得一并确认）：
  `verify_ui` **8/8**、`verify_ops` **9/9**、`verify_semantics` **5/5**、`verify_new_materials_ui` **2/2**、
  `verify_vertical` ✅、`verify_materials` ✅（12.000s、A/V 镜像 True、0 警告）。
- 补跑完剩下两个：`verify_m2` ✅（YOLO / ASR 均可用，4 视觉段 / 4 转写段）、
  `verify_asr_guard` ✅（素材2 十一段、素材3 纯音乐为空 —— 护栏在所有改动后仍有效）。

**用户入口脚本（2026-09-24 核对）**
- `一键测试.bat` 原写死 `Done. Expect: 40 passed`，而实际已是 **163** —— 从只有 40 个用例那时起
  就没再更新，用户双击跑完会看到一个**错误且会持续误导**的数字。
  → 改成**按退出码判定**（`if errorlevel 1` → FAILED，否则 ALL PASSED + 指向上方统计行），不再写死条数。
  **会过时的数字就不要写死** —— 这是同一个位置第二次犯这个错。
- 两个 bat 原本是纯 ASCII，本轮加了中文提示 → 补 `chcp 65001`（Windows 默认 GBK 会显示成乱码）。
- `一键启动界面.bat` 补一句：7860 被占用时会自动改用邻近端口，以窗口实际打印为准
  （`ui._pick_free_port` 的行为，见 Bug E）。
- 注：`一键测试.bat` 需 `cmd.exe` 执行，本环境禁止调用 cmd，故**未实跑**；
  其调用的 `.venv\Scripts\python.exe -m pytest tests -q` 已单独验证（163 passed、退出码 0）。

- 顺带记录一个**已知边界**（不修，如实标注）：`delete_range` 对越界区间是**静默截断**的
  （如 24s 素材上传 `(0,40)` 会删光且库层 0 警告）；只有走到 UI 才会被
  「操作把视频片段全删光」的安全闸拦下（实测给出 ❌ 未编译、不产出）。
  部分越界（删多了但没删光）则没有任何警示 —— 这类只能通过语义事实核对（Bug R 那道闸）覆盖。
- 交付文档第 8b 节有完整记录；**遗留产品决策**（是否自动改用规则结果）待用户拍板。

**A/V 逐段镜像（多区间删除后的拼接方式）**
- 素材2 删除 3 个公交镜头后，保留片段 `[(4,8),(12,16),(20,24)]`；视频用滤镜 `concat=n=3:v=1:a=0` 串接、
  音频用 `concat=n=3:v=0:a=1` 串接，两轨段数与边界逐一对应 → 实测 A/V 完全对齐、**0 警告**。

**样例成片**（真实渲染，存放于 `outputs/`）：
- `样例_删掉bus片段_加字幕.mp4`（**5.00s**，公交镜头整段删除 + 字幕）★ 主验证物
- `样例_素材2_删掉说公交车的片段.mp4`（同素材同结果，但走的是**台词**路径 —— 转写文本检索 +
  区间外扩到镜头；文件与上图那份**字节数完全相同**，这就是两条独立路径收敛的直接证据）
- `样例_素材2_删掉三个bus.mp4`（新素材2：24s → **12.00s**，3 个公交镜头全删，走**视觉**路径）
- `样例_去掉前2秒.mp4`（13s）、`样例_加字幕.mp4`（15s）、`样例_字幕预览.png`
- `样例_字幕去重验证.mp4`（15s，同一条字幕下两遍 → 只渲染一层）+ `验证_字幕单层.png`
- `验证_竖屏字幕.png`（720×1280 竖屏，字幕完整落在画面内 —— Bug O 的验证物）
- **Bug M 修复后的新样片**：`样例_黑白效果.mp4`、`样例_淡入淡出转场.mp4`、
  `样例_背景音乐混音.mp4`、`样例_片段重排.mp4`、`样例_淡入效果.mp4`（均 15s，真实素材）

**全链路实测**（`测试素材.mp4`）：① 摄取 2.6s → ② M2 首次 44.3s / 二次 4.2s → ③ 规划 → ④ **编译 + 真实渲染**。
- 「删掉有bus的片段 + 加字幕"开场"」→ 成片 **5.00s**（公交镜头整段删除），A/V 逐段镜像。
- 新素材走 **UI 处理器**全链路（`_verify/verify_new_materials_ui.py`）：素材2 → **12.00s / 1280×720**；
  素材3（竖屏）→ **18.00s / 720×1280**，均 PASS。

## 混合策略定稿（实测驱动 + 用户最终确认）

早期设想"本地优先+云端兜底"，但实测本地 Qwen3-0.6B 质量不足（错答/空数组/漏字段），故翻转为 **云端优先+本地兜底**：
- 云端 `DeepSeek-V4-Flash`（SiliconFlow）保证编辑正确；
- 本地 `Qwen3-0.6B` 仅在云端不可达/超时时作离线安全网（`HybridProvider` 对本地输出做 `_is_valid_ops` 校验，不可解析仍兜回云端）。

**用户最终确认（2026-09-20）**：本机跑不动更大模型，"用联网的就好了"。→ 日常主用**联网云端（SiliconFlow）**；本地 Qwen 仅作可选离线兜底，**默认不启动**（需要时双击 `start_qwen.bat`）。**UI 默认模式已改为「仅云端 SiliconFlow」（2026-09-20 落实）**，不再默认混合。

## 踩坑记录

- Gradio 升级到 **6.27.0** 后 `gr.Code` 语言集收紧：`"text"` / `"bash"` 不再支持，改为 `"shell"`（命令）/ 省略语言（默认 `None`）；`"json"` 仍可用。
- `gr.update` 在 Gradio 6 已不稳定，重置视频改用返回 `None` 复位。
- 本地 Qwen 服务端口选 **8001**（与 PAI 文生图 8000 错开）；用 `Qwen_Model/deploy` 的 GPU torch 2.13 cu132 venv 加载 0.6B（fp16 ~1.2GB 显存），与已加载的 PAI 服务共存无忧。
- **别让 LLM 自己"记"时间轴结构**：只给 timeline JSON 时它会把示例里的 `src_video` 当成万能目标；必须显式给「可用 id 清单 + 素材事实 + 派生命名规则」，且多区间场景要给 worked example。
- **`cut` 的 `__a`/`__b` 派生是公开契约**：规则 Planner、提示词示例、别名解析表三者共享同一约定，改命名会同时打破三处。
- **本环境后台进程跨工具调用会被杀**：`nohup ... &` 起的 Gradio 服务在本次调用结束即消失；要测服务端链路必须在同一个脚本里 `launch(prevent_thread_lock=True)` + `gradio_client` 一气跑完（已验证可行）。
- **Gradio 不会热加载 `.py`**：改完代码必须重启界面进程，否则测试全绿但界面仍是旧逻辑（本次 bug 修复也需重启才生效）。
- **"只生成命令、不执行"的脚手架会掩盖真 bug**：`compiler.compile()` 长期只产出命令字符串，所以单音频滤镜链拼接错误（G1）一直没暴露；一旦真跑 ffmpeg 立刻报 `Trailing garbage after a filter`。**教训**：涉及外部工具的命令，务必真跑一次；为此补了 `tests/test_render.py`（真出片 + ffprobe 校验）。
- **`Clip` 字段名不对称**：是 `in_` / `out`（`out` **无**下划线），`sourceIn` / `sourceOut` 有驼峰。写同步逻辑时误写 `o.out_` 导致尾部不同步，单测当场抓出。
- **ffmpeg 没进 PATH**：项目自带 `tools/ffmpeg/bin/ffmpeg(.exe)`，`render.resolve_ffmpeg()` 按「显式 → `FFMPEG_BIN` → 项目自带 → 系统 PATH」定位；`compile()` 的 `argv[0]` 是字面量 `"ffmpeg"`，`render()` 会替换成绝对路径，否则 `FileNotFoundError`。
- **Gradio 的 `click(outputs=[...])` 是位置契约，没有键名保护**：给输出列表插入新组件时，若返回元组没同步插入同一位置，就会整批错位且**测试全绿**（旧测试按函数自己的顺序解包，对"接线顺序"无感）。**对策**：顺序只留一个来源（`APPLY_OUTPUT_KEYS` 常量 + 内部 `out(**kw)` 摊平），并用 `build_app().fns` 拿**真实接线组件按类型逐位断言**。
- **Gradio 一个输出后处理失败 → 该依赖的全部输出显示为"错误"卡，且卡里通常没有原因**：表现为"整块变红"，很容易误判成后端崩了（实际后端成功）。**对策**：`launch(show_error=True)` 让真实异常显示出来；排查时先用 `gradio_client` 打一次同名端点，看服务端到底抛没抛（本例 `AppError` 一测即知）。
- **`build_app().fns` 是校验接线的利器**：`{fn_id: BlockFunction}`，可拿到每个处理函数实际接的 `outputs` 组件列表与 `fn` 本身，用来做"输出数 == 返回标注元数"、"第 N 位必须是 gr.Video"这类结构性断言。
- **不要指望 LLM 跨多步推演派生 id**：即使给了 worked example，多区间场景仍会推歪，而且**非严格模式下推歪的后果是「静默跳过」**——界面显示"已应用"，实际只做了一半。**正确做法是提供免 id 的范围型操作**（`delete_range: {start,end}`），让模型只填业务坐标。**判断标准：提示词里出现"id 按上一步推演"字样，就是设计错了。**
- **配对同步要下沉到"所有改素材区间的操作"**：只给 `trim` 做 A/V 同步是不够的，`cut` / `delete_clip` 同样必须同步配对轨，否则视频剪对了、音频还是原时间轴。
- **`amix` ≠ 拼接**：多段音频"顺序拼接"必须用 `concat=n=K:v=0:a=1`；`amix` 是并行混音（总长只取最长段），只适用于 BGM 叠加。
- **"时长相等"不能证明 A/V 同步**：`-shortest` 会按较短的流截断，把错位的成片伪装成"时长正常"。要证明剪辑真生效，得做**内容溯源**（静音位置比对 / 音频窗口互相关 / 场景切换点 / 逐帧静止段结构）。
- **「操作改了 Timeline」≠「成片变了」**：契约层收下的操作，渲染层可能根本没读。本次 `effects` / `transitions` / BGM 三块数据编译器完全没消费，四类操作空转了很久，而 75 个用例全绿——因为测试只覆盖了中间态（Timeline 字段、命令字符串），**没有一条断言最终产物**。**对策**：加「操作→成片」层的结构性护栏（`test_every_op_changes_compiled_output`：每个操作应用后编译产物必须变化）+ 真渲染的像素/PCM 差分。
- **验收要问「成片哪里变了」，而不是「有没有报错」**：被问「功能齐不齐」时，把 9 类操作逐个真跑一遍出片矩阵（ffprobe 时长/流 + 抽帧像素 + PCM 差分），比读代码或看测试绿灯可靠得多——本次 4 个空转操作就是这么揪出来的。
- **`amix` 做 BGM 混音要带 `normalize=0`**：否则 ffmpeg 会按输入路数把音量**除下去**，主音频被莫名压低；长度用 `duration=first` 跟主音频走。
- **「命中关键帧」≠「命中镜头」**：把检测结果映射成编辑区间时，粒度必须是**镜头**。按关键帧跨度删会两处犯错：同镜头残留未覆盖画面（用户看到的还是"没删干净"），以及单关键帧命中产生**零长度区间** `(t,t)` —— 而 `delete_range` 要求 `start < end`，严格模式报错、非严格模式静默跳过。**区间语义必须完整到用户能验收的程度**。
- **验证脚本别自己重写项目已有的实现**：我曾在本轮验证脚本里另写了一套 ffmpeg `select='gt(scene,..)'` 场景检测，结果为 0 个切点，而项目自己的 OpenCV 版同一素材给出 5 个 —— 差点把"脚本实现 bug"误判成"素材没内容"。复用被测代码本身（`ingest.scene_cuts`）才是验证。
- **字幕尺寸必须显式声明坐标系**：ASS 不写 `PlayResX/PlayResY` 时 libass 按 384×288 解释，声明的 `Fontsize` 变成"画面高度的百分之几"——横屏碰巧正常、竖屏直接溢出被裁。**光看字号数值判断不出来**，必须先把坐标系钉死，再用"字号 × 字数 ≤ 可用宽度"做断言。同时 `WrapStyle` 要是 0（允许折行），否则长字幕不折行一律溢出。
- **竖屏/异形分辨率要单独验**：同一个字幕 bug 在 1280×720 上完全看不出来，换成 720×1280 才暴露。所有"按宽高算"的逻辑（字幕、水印、缩放、留边）都该有一份竖屏素材去照。
- **别把「模型的幻觉」当成素材的台词**：20s **纯音乐**素材被 FunASR 转成 `'the'`，又因退化分支取整片时长而放大成 `0–20s "the"`。护栏要**保守**——只丢"结构上不可能成立"的段（覆盖近全片 + 不含汉字 + 词数 ≤ 2），中文整段旁白必须保留。同时实测否掉了两个想当然的假设：`sentence_timestamp=True` **不生效**（返回体只有 `{'key','text'}`）、把 `vad_model="fsmn-vad"` **挂进 `AutoModel`** 既治不了幻觉也拿不到时间戳。**先 dump 真实返回结构，再决定要不要下模型。**
- **同一个模型，"挂上去"和"单独用"能拿到的东西不一样**：把 `fsmn-vad` 挂进 `AutoModel(vad_model=...)` 时，它只是内部切段后**又把结果拼回一条**返回 —— 段边界被吞掉，所以看起来"VAD 没用"。**单独实例化** `AutoModel(model="fsmn-vad")` 才能拿到 `value == [[起ms,止ms],…]`。拿到边界后逐段切音频逐段转写，就同时解决了"没有时间戳"和"文本幕序错乱"两个问题（素材2：整片一条 → 3 段，且内容与时间戳一一对应）。**结论：中间层的方便封装常常以"丢掉结构化信息"为代价；需要细粒度信息时，绕开封装直接调底层。**
- **VAD 不是"语音有无"的裁判**：实测纯音乐素材 VAD 返回 `[[0, 19980]]` —— 整段判成语音。所以"有 VAD"≠"没幻觉"、"VAD 切出段了"≠"真的有语音"。凡是靠某个中间模块的输出做"有/无"判断，都要用**已知为负的样本**测一遍（这里就是"纯音乐"素材）。
- **参数传了不等于生效**：本轮用 `vad_post_args={"max_end_silence_time": 300}` 传参，四个不同阈值（800/500/300/200）跑出**完全相同**的结果 —— 差点据此得出"静默阈值不影响分段"的错误结论。查源码才发现 `FsmnVADStreaming.__init__` 里有一行 `self.vad_opts = VADXOptions(**kwargs)`：**认的是直接关键字**，签名里那个 `vad_post_args` 从未被使用（半成品参数）。改用直接关键字后，同一素材的分段从 3 段变 11 段。**教训：调参时若"怎么调都没变化"，先做一次"极端值对照"确认参数链路是否真的通，再下结论。** 更省事的做法是翻一眼调用点，看这个参数到底喂给了谁。
- **同一个修复可能在不同数据上反向**：把 VAD 阈值调细解决了"幕被合并"，却在纯音乐素材上**制造了新幻觉**（1 段 → 6 段，且单段判据全部漏过）。**参数调优必须拿全部测试素材（含"负样本"）回归一遍**，不能只看目标素材变好了。
- **先量源素材再下结论**：本次差点把"源素材本身是静态幻灯片"误判成"渲染冻结帧 bug"。**逆向还原用户成片（它到底是源的哪几段拼起来的）比读代码猜快十倍。**

## 运行

```bash
cd E:/多媒体融合剪辑/ai-video-agent
.venv/Scripts/python.exe -m ai_video_agent.ui   # http://127.0.0.1:7860
# ④ 应用并编译 → 成片输出到 outputs/<时间戳>/output.mp4（界面 gr.Video 可直接预览/下载）
# 可选本地 Qwen（离线兜底）：双击 E:\AI_models\Qwen_Model\start_qwen.bat -> 8001/v1
```

## 下一步（见交付文档第 7 节）

1. **真 LLM 混合后端**：✅ 已实装并实测两端可用（云端 SiliconFlow + 本地 Qwen3-0.6B@8001），策略定稿为「云端优先+本地兜底」。
2. ~~M2 FunASR 真实转写~~：✅ 已完成（带音轨样本实跑 Paraformer 成功，见 `M2_VERIFICATION.md`）。
