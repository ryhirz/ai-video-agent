# AI 视频剪辑智能体 · 一键配置与运行说明

> 给老师/验收同学的一页纸交付指南。照着「环境要求 → 一键配置 → 验证 → 启动」四步走即可，无需懂代码。

---

## 一、这是什么

一个**用自然语言指挥视频剪辑**的本地智能体：输入"删掉有公交车的片段""把字幕改成竖屏 9:16"，系统自动规划剪辑操作、调用 ffmpeg 出片。
配套有 163 个单元测试、22 个验收脚本，以及本说明同包的**汇报 PPT**（`AI视频剪辑智能体_汇报.pptx`）。

## 二、目录结构（解压后）

```
AI视频剪辑智能体/
├─ ai_video_agent/       核心源码（11 个模块：agent/understand/ui/render…）
├─ tests/                163 个单元测试（pytest）
├─ _verify/              22 个可复跑验收脚本（证明"真能出片"）
├─ scripts/              素材生成等辅助脚本
├─ tools/ffmpeg/bin/     ★ ffmpeg 工具链（已打包，约 472MB，出片必需）
├─ weights/              yolov8n.pt 视觉模型权重（已打包）
├─ requirements.txt         基础依赖（必装）
├─ requirements-full.txt    完整依赖（含 AI 模型，可选）
├─ 一键配置环境.bat       ★ 一键装基础环境
├─ 一键配置环境_完整版.bat ★ 一键装完整环境（含 AI 模型）
├─ 一键测试.bat          ★ 跑全部测试
├─ 一键启动界面.bat      ★ 启动 Web 界面
├─ 交付文档.md / 交付概览.md / overview.md   详细设计文档
└─ AI视频剪辑智能体_汇报.pptx   汇报演示稿
```

## 三、环境要求

- **操作系统**：Windows 10/11（本包按 Windows 路径打包，ffmpeg 为 `.exe`）
- **Python**：3.10 及以上（开发验证用 3.13；装 3.11/3.12 亦可）。安装时**务必勾选 "Add Python to PATH"**

## 四、一键配置（最关键的一步）

1. 进入解压后的项目根目录。
2. **双击 `一键配置环境.bat`**（基础版）：
   - 自动检测 Python → 建虚拟环境 `.venv` → `pip` 安装 `requirements.txt`（gradio/numpy/opencv，约数百 MB）→ 校验 ffmpeg 与关键库。
   - 全程中文提示，失败会明确报错并停留，方便排查。
3. （可选）若要启用**真实 AI 理解**（YOLO 视觉检测 / 语音转写）并跑满 163 测试：
   - 双击 `一键配置环境_完整版.bat`，多装 `torch + ultralytics + funasr`（约 2~3GB，较慢，首次运行还需联网下载模型权重）。

> 配置只需做一次。之后开/关电脑、重新演示，直接双击下面两个 bat 即可，不用重配。

## 五、验证（确认跑得通）

双击 **`一键测试.bat`**：

- **基础环境**：显示 `161 passed, 2 skipped, 27 subtests passed`（那 2 个是被跳过的 YOLO 真实检测用例，正常）。
- **完整环境**：显示 `163 passed, 27 subtests passed`，全部通过。

脚本按**退出码**判定成败（不再写死数字），看到 `ALL PASSED` 即成功。

## 六、启动界面（演示用）

双击 **`一键启动界面.bat`**：

- 自动清理上次遗留的旧 UI 进程，启动 Gradio 服务。
- 浏览器打开 **http://127.0.0.1:7860**（若 7860 被占用会自动顺延，以窗口打印的实际地址为准）。
- 关闭那个黑色命令行窗口即停止服务。
- 界面里用「规则 Planner（离线）」模式即可离线体验完整剪辑规划；「云端 Endpoint」模式需自备兼容 `/chat/completions` 的 LLM 地址；「② M2 理解」按钮需完整环境。

## 七、常见问题

| 现象 | 原因 / 解决 |
|---|---|
| 双击 bat 一闪而过 / 提示"未检测到 Python" | 安装 Python 时没勾 "Add to PATH"，重装并勾选，或用 `py` 启动器 |
| 测试里 2 个用例 skipped | 正常：那是 YOLO 真实检测，需要完整依赖；基础环境会跳过 |
| 界面能开，但"② M2 理解"提示依赖未安装 | 基础环境不含 AI 模型；双击 `一键配置环境_完整版.bat` 即可 |
| 真实出片失败 / 测试报找不到 ffmpeg | 解压时 `tools/ffmpeg/bin/` 三个 exe 没解出来；重新解压或单独放置 ffmpeg 到该目录 |
| 完整环境 `pip` 下载慢 | torch/funasr 较大，属正常；可换国内镜像：`pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple` |

## 八、交付物清单

| 文件 | 说明 |
|---|---|
| `AI视频剪辑智能体-源码.zip` | **本包即全部交付物**：源码 + 测试 + 验收脚本 + 文档 + ffmpeg 工具链 + 模型权重 + 4 个一键 bat + 汇报 PPT，解压即用、可离线 |
| └ `AI视频剪辑智能体_汇报.pptx`（包内） | 14 页汇报演示稿（项目介绍 / 技术栈 / 架构 / 5 大缺陷修复 / 性能优化 / 测试验收），已随包一起打包 |
