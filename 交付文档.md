# AI 视频剪辑智能体 · 交付 / 重启上下文文档

> 用途：把当前进度完整沉淀，供在新对话框粘贴以**无缝重启任务**。
> 最后更新：2026-09-23 · 状态：M1–M5 已闭环验证（全量 **163 passed** + 27 subtests）；M2 Paraformer 真实转写已跑通；M5 混合后端定为「云端优先(SiliconFlow/DeepSeek-V4-Flash)+本地兜底(Qwen3-0.6B)」，两端均验证可用；④「应用并编译」已**真正出片**（`render.py`）；**2026-09-23 修复 M5 实跑暴露的一串 bug**：操作链引用已消失 id、LLM 输出不可解析、M2 点按无反馈、端口占用、④ 输出位置契约错位、**多区间删除被静默跳过（J）**、**音频不跟随剪辑导致 A/V 失步（K）**、字幕重影（L）、**4 类操作只改时间轴不影响成片（M）**、**语义删除只删关键帧跨度而非整个镜头（N）**、**竖屏/小分辨率下字幕横向溢出被裁（O）**，并给 M2 转写加了**纯音乐幻觉护栏（P）**（详见第 8b 节 A–P）；**2026-09-24 补修提示词契约（Q）**——退化事实未自报家门导致提示词对模型说假话、且提示词内容此前无任何断言；**并用本地真实模型（Qwen3-0.6B）首次跑通 LLM 规划路径（R）**——发现模型会少删/多删且不报错，已加「确定性事实核对」闸（详见第 8b 节 Q–R）。

---

## 0. 一句话定位

把"自然语言剪辑指令"变成"结构化 Edit Operation"，再由**确定性编译器**翻译成 FFmpeg `filter_complex` 命令——AI 只发结构化操作，编译纯函数可单测/回放。这是 MVP（方案 A：YOLOv8 视觉理解）。

**它不是传统前后端 Web 应用**：是本地智能体库 + 确定性编译器。所谓"后端"=`compiler.py`/`operations.py` 纯函数；"前端"=Gradio 界面（M5）。无服务器、无数据库。

---

## 1. 里程碑状态

| 里程碑 | 内容 | 状态 | 验证 |
|--------|------|------|------|
| M1 摄取 | ffprobe 元数据 + 镜头检测 + 关键帧 + 桥接初版 Timeline | ✅ | test_ingest ×3 + test_pipeline ×1 |
| M2 理解 | YOLOv8 视觉段 + FunASR(Paraformer) 转写回填 Footage Index（**VAD 分段给出段落级时间戳**） | ✅（YOLOv8 真实跑通；Paraformer 真实转写已验证；VAD 分段实测与幕边界对齐；模型进程级缓存） | test_understand ×29（YOLOv8 + VAD 段解析 + 两级幻觉护栏）+ scripts/paraformer_demo.py（真实语音转写） |
| M3 契约（冻结） | Timeline 单一真相 + **9 类操作**纯函数 + 确定性编译器 + 批量应用鲁棒层 | ✅ 冻结 v1.0.0 | test_contract ×14 |
| M4 智能体 | NL → 结构化操作（EditingAgent + RuleBasedPlanner + JsonPlanner + LLMProvider 接口）；**新增按台词删除** | ✅ | test_agent ×24 + test_robust_ops ×29（提示词上下文 / JSON 容错 / 重试） |
| M5 UI | Gradio 演示界面（含云端+本地混合 LLM 后端） | ✅ | 手工起服 + gradio_client 全链路实测 + test_ui ×21（build_app / do_plan+do_apply_compile / 语义检索 / 降级 / LLM 提供方可构造 / 真 LLM stub 端到端 ×2 / HybridProvider 路由 ×4 / _is_valid_ops 校验 ×5 / **输出契约 ×3**） |
| **出片覆盖（2026-09-23 新增）** | **9 类操作全部真实渲染**（BGM 混音 / 画面效果 / 转场 / 片段重排） | ✅ | **test_render_ops ×9（含「操作→成片」结构性护栏 + 像素/PCM 差分）** |

**全量回归：163 passed + 27 subtests** = test_agent 43 + test_contract 14 + test_ingest 3 + test_pipeline 1 + test_understand 29 + test_render 3 + **test_render_ops 9** + test_robust_ops 33 + test_ui 28。

---

## 2. 三架构铁律（冻结，不可破坏）

1. **Timeline 单一真相**：所有编辑状态唯一的 JSON/EDL。
2. **AI 只发结构化操作**：Agent 产出 `{type, payload}`，绝不直出 ffmpeg 字符串。
3. **确定性编译器可单测/回放**：`compiler.compile(tl)` 是纯函数，同输入同输出。

> 补充（2026-09-23）：操作的应用仍是**确定性纯函数**；新增的 `apply_operations()` 只是"引用解析 + 逐条应用 + 失败收集"，不改契约、不引入随机性，同输入仍得同输出。

---

## 3. 数据流

```
原始视频
  → ingest.build_footage_index()          [M1] 元数据+场景+关键帧时间戳
  → understand.build_visual_understanding()[M2] YOLOv8 视觉段 + FunASR 转写 回填 Index
  → agent.EditingAgent.plan(nl, tl, idx)  [M4] NL → [{type,payload}]
       （JsonPlanner 提示词注入：可用 clip id 清单 + M2 事实 + cut 派生命名规则）
  → operations.apply_operations(tl, ops)  [M3] 派生 id 解析 + 逐条纯函数应用 → 新 Timeline
  → compiler.compile(tl)                  [M3] Timeline → ffmpeg filter_complex 命令
```

---

## 4. 文件地图（绝对路径，E:\多媒体融合剪辑\ai-video-agent\）

| 文件 | 职责 |
|------|------|
| `ai_video_agent/timeline.py` | Timeline/Clip/Subtitle/Transition 数据模型（in_↔"in" 序列化映射） |
| `ai_video_agent/footage_index.py` | FootageIndex（metadata/scenes/keyframes/visual_segments/transcript_segments） |
| `ai_video_agent/ingest.py` | M1：probe / scene_cuts / extract_frames / build_footage_index / build_initial_timeline；`ffmpeg_bin_dir()` 已修（优先 `<项目>/tools/ffmpeg/bin`） |
| `ai_video_agent/understand.py` | M2：`detect_objects`(YOLOv8) / `transcribe`(FunASR) / `build_visual_understanding`；**`_YOLO_CACHE`/`_ASR_CACHE` 模型缓存 + `disable_update=True`**；`_scene_index_at`/`_label_ranges_from_times`（语义删除区间外扩到镜头，Bug N）；`_looks_like_hallucination`（ASR 幻觉护栏，Bug P）；`speech_delete_degrades_to_raw_segments`（按台词删除的**降级回显**判定，Bug Q）；`expected_semantic_ranges`/`semantic_delete_discrepancies`（**确定性事实核对**——拿 M2 事实核对模型的删除区间，Bug R）；`_get_vad`/`_vad_spans`/`_slice_wav`（**单独实例化 VAD** 取语音段边界 → 逐段转写，得到段落级时间戳，Bug P） |
| `ai_video_agent/operations.py` | M3：`apply_operation` 分发 8 handler（纯函数）+ **`apply_operations` 批量鲁棒层（派生 id 别名表 / 失败收集 / `OperationError`）** |
| `ai_video_agent/compiler.py` | M3：`compile(tl)` → {command, video_filter, audio_filter, ass}，纯函数 |
| `ai_video_agent/agent.py` | M4：`EditingAgent` / `RuleBasedPlanner` / `JsonPlanner` / `LLMProvider`(Protocol)；**`_SCHEMA_HINT` 重写（iron rules + 单/多区间 worked example）、`timeline_digest()`、`facts_block()`、`_extract_json_array()` 容错解析 + 失败重试一次**；`_speech_ranges`/`_speech_by_shot`（**按台词删除**：台词区间外扩到镜头 + 事实块按镜头聚合台词），`facts_block` 台词段改为聚合输出 |
| `ai_video_agent/model_client.py` | LLM 端点选型层（Qwen-VL 主 / GLM / DeepSeek / Kimi / Doubao 路由，纯配置，不发请求） |
| `ai_video_agent/ui.py` | M5：Gradio `gr.Blocks` 单页演示（`build_app()`；`do_ingest/do_understand/do_plan/do_apply_compile/do_reset` 模块级处理器）；`OpenAICompatibleProvider`（stdlib urllib 真 LLM）+ `HybridProvider`（**云端优先+本地兜底**）；**应用/编译走 `apply_operations` 非严格模式 + 「视频被删光」安全闸** |
| `ai_video_agent/__init__.py` | 包入口 |
| `tools/ffmpeg/bin/{ffmpeg,ffprobe,ffplay}.exe` | 静态 ffmpeg（194MB，已就位） |
| `yolov8n.pt` + `weights/yolov8n.pt` | YOLOv8 nano 权重（6.35MB，双份，magic PK.. 有效） |
| `scripts/paraformer_demo.py` | M2 真 FunASR(Paraformer) 端到端验证脚本（带音轨样本→转写） |
| `一键测试.bat` | Windows 双击跑全量测试（内部 `cd /d "%~dp0"` 定位项目目录，规避 cmd 切盘/斜杠坑） |
| `一键启动界面.bat` | Windows 双击启动 Gradio 演示界面（关窗口即停服务） |
| `测试素材.mp4` | **演示用测试视频**：3 幕真实实拍图（公交→人物→公交），15s/1280×720/h264+AAC 中文旁白；可直接在 UI 上传（M2「删掉有bus的片段」可验证） |
| `scripts/make_demo_clip.py` | 生成 `测试素材.mp4` 的脚本（ffmpeg 多幕拼接 + edge-tts 旁白；每个输入用 `-loop 1 -t SEC` 限长，规避 zoompan 无限循环） |
| `测试素材2_多目标.mp4` | **测试素材**：6 幕 24s / 1280×720，公交与人物交替（**3 个公交镜头** 0–4 / 8–12 / 16–20s），逐幕中文旁白与幕严格对齐。专测**多区间删除**与 A/V 溯源 |
| `测试素材3_竖屏音乐.mp4` | **测试素材**：5 幕 20s / **720×1280 竖屏**，zoompan 真实推镜 + numpy 合成的**纯音乐音轨（无语音、无静音段）**。专测竖屏路径、ASR 空转降级、连续音频溯源 |
| `scripts/make_more_clips.py` | 生成上述两段素材的脚本（幕间**硬切**不加淡入淡出——加了会让 1Hz 场景检测把 1 个切点算成 2 个且关键帧落在黑帧上） |
| `outputs/验证_竖屏字幕.png` | 竖屏成片抽帧：证明字幕完整落在 720×1280 画面内（Bug O 修复前两端被裁） |
| `E:\AI_models\Qwen_Model\serve_qwen3.py` | 本地 Qwen3-0.6B OpenAI 兼容服务（FastAPI+transformers，GPU/cu132，端口 8001，与 PAI 文生图 8000 错开） |
| `E:\AI_models\Qwen_Model\start_qwen.bat` | 启动本地 Qwen 服务（用 `Qwen_Model/deploy` GPU venv；双击即开、关窗口即停，不自启不常驻） |
| `tests/test_contract.py` `test_ingest.py` `test_pipeline.py` `test_understand.py` `test_agent.py` `test_ui.py` | 各层测试 |
| `tests/test_robust_ops.py` | **鲁棒性回归（29 用例）**：stale clip id 解析 / 歧义报错 / 未知 id 只跳过不中断 / strict 模式 / 严格模式 / JSON 容错解析 / 重试 / 提示词含 id 与 M2 事实 / **A/V 同步 ×7** / **编译音频图** / **字幕去重 ×2** |
| `tests/test_render_ops.py` | **「操作→成片」回归（9 用例）**：`test_every_op_changes_compiled_output`（9 类操作应用后编译产物必须变化，防"空转操作"的结构性护栏）+ 真渲染像素/PCM 差分（黑白去色 / 转场变暗 / BGM 混入 / 重排可见）+ 未知效果不得静默 |
| `_verify/` | 验收脚本（可复跑，**素材夹具 `_fixture.py` 会在首次运行时自动生成**，删掉也能自愈）：`verify_ops.py`（9 类操作出片矩阵）/ `verify_semantics.py`（像素/音频差分）/ `verify_ui.py`（UI 端到端）/ `verify_m2.py`（M2 真实理解）/ `demo_new_features.py`（真实素材样片）/ `verify_materials.py`（测试素材规格+场景+静音+运动+YOLO+端到端）/ `verify_vertical.py`（竖屏链路专项）/ `verify_new_materials_ui.py`（新素材走 UI 处理器全链路）/ `demo_clip2.py` / `redemo_samples.py`（样片重出）/ `probe_asr.py`（FunASR 原始返回结构探测）/ `probe_vad.py`（挂 VAD 后返回结构探测，实测证否"VAD 能治幻觉/拿时间戳"）/ `probe_vad_segments.py`（**单独实例化 VAD** 探测段边界，确认 `[[起ms,止ms],…]`）/ `probe_vad_thresholds.py`（VAD 静音阈值对照实测，确认参数要**直接关键字**传）/ `verify_asr_guard.py`（ASR 护栏 + VAD 分段实测：素材2 → 11 段与幕边界对齐、素材3 → 空）/ `verify_speech_delete.py`（**按台词删除**端到端：台词路径 vs 视觉路径交叉验证 + 真出片）/ `demo_speech_delete.py`（出「按台词删除」样片到 `outputs/`）/ `bench_transcribe.py`（分段 vs 整片一次的耗时 A/B）/ `bench_batch_asr.py`（批量 vs 逐条转写）/ `bench_numpy_slice.py`（**内存切片 vs ffmpeg 切段**，3.7× 提速的实测依据）/ `verify_llm_real.py`（**真机 LLM 端到端**：连真实端点跑完整链路，并按 M2 事实核对模型的删除区间 —— Bug R 的发现与验证脚本）/ `verify_ui_llm_real.py`（**真模型 + 真 App 路径 + 真出片**：`do_plan` → `do_apply_compile` → 渲染 → ffprobe，把两种后端的成片时长摆到一起对比） |
| `tests/.cache/bus.jpg` | 测试图（拷自 ultralytics 包内，137KB） |
| `tests/.cache/sample_with_audio.mp4` | 带音轨测试片（ffmpeg 由 testsrc 视频 + edge-tts 中文语音混流，AAC 音轨） |
| `M2_VERIFICATION.md` `M4_VERIFICATION.md` `M5_VERIFICATION.md` | 验证报告 |

---

## 5. 关键决策与假设（重启必读）

- **M4 不引入 LangGraph 重依赖**：改轻量自定义 `EditingAgent`（沙箱装重包网络成本高，且轻量版现已离线验证整链）。真 LLM 通过 `LLMProvider` 协议热插拔。
- **LLM 后端（2026-09-20 实测后定稿，用户最终确认）**：默认 `RuleBasedPlanner`（离线无需 Key）；选「混合」走 `HybridProvider`（**云端优先+本地兜底**）——推荐，日常即用云端。原因：实测本地 `Qwen3-0.6B` 太弱，对"去掉前2秒"会错答为 `delete_clip` 或空数组、"在第2秒切一刀"漏 `at` 字段；而云端 `DeepSeek-V4-Flash` 经 SiliconFlow 正确率高。**用户确认本机跑不动更大模型，"用联网的就好了"** → 本地 Qwen 仅作可选离线兜底，默认不启动（需要时双击 `start_qwen.bat`）。选「仅云端/仅本地」走对应 `OpenAICompatibleProvider` → `JsonPlanner`。云端 Key 经 UI 填写或环境变量 `DEEPSEEK_API_KEY` 注入；本地端点默认 `http://127.0.0.1:8001/v1`（模型 `qwen3-0.6b`）。
- **UI 默认模式 = 仅云端 SiliconFlow**（2026-09-20 用户要求从「混合」改为「仅云端」）。
- **FunASR 真实转写已验证（2026-09-19）**：用 edge-tts 合成中文语音 → ffmpeg 混流 `tests/.cache/sample_with_audio.mp4` → `understand.transcribe()` 跑 Paraformer（`paraformer`/`v2.0.4`）成功转写，RTF≈0.356（CPU 2.2s/6.24s）。复现见 `scripts/paraformer_demo.py`。推理需 fbank 后端 `kaldi-native-fbank`（已装，替代 torchaudio）。
- **M2 MediaPipe 已替换为 YOLOv8**：Py3.13 无 MediaPipe wheel，人脸/姿态统一用 YOLOv8 系列。
- **cut 的 id 派生约定是公开契约的一部分**（2026-09-23 明确）：`cut` clip `X` 产生 `X__a`（前半）/`X__b`（后半），原 id 消失；规则 Planner、提示词示例、别名解析表三者都依赖这个约定，**不要改命名规则**。

---

## 6. 沙箱网络踩坑（重启必看，避免再花 20 分钟）

- **pip 源**：venv 已锁 `ai-video-agent/.venv/pip.ini` → `mirrors.aliyun.com/pypi/simple/`。若重装包，沿用此源（tuna/官方 JSON 变体被代理拦成空 200）。
- **GitHub 文件 CDN 不可解析**：`release-assets.githubusercontent.com` 在本沙箱 DNS 不可解析 → ultralytics 自动下载 `yolov8n.pt` 必挂。**权重已落盘，勿再触发自动下载**。
- **公开 GitHub 代理**：`ghproxy.net` 可慢速续传（~17KB/s，支持 `-C -`）；`ghfast.top`/`gh-proxy.com` 不可用（HEAD 200 但 GET 0 字节）。`raw.githubusercontent.com` 直连可达。
- **HF 镜像 / ModelScope**：均无官方 `ultralytics/yolov8n` 仓库。
- **ffmpeg**：已就位，无需再下。
- **FunASR/Paraformer**：`torch`+`funasr`+`modelscope` 已装；Paraformer 权重走 **ModelScope**（`iic/speech_paraformer-large_asr_nat-zh-cn-16k-common-vocab8404-pytorch@v2.0.4`）本沙箱**可达且下载成功**（缓存于 `D:\Dev\Cache\modelscope`）。推理需 fbank 后端，已装轻量纯 C 回退 `kaldi-native-fbank`（替代 `torchaudio`，免 ffmpeg 依赖）。中文语音样本用 `edge-tts`（微软在线 TTS，国内可达）合成。**`AutoModel` 记得传 `disable_update=True`**，否则每次加载都联网查版本。
- **云端 LLM 网关**：用户 Key（`sk-mzf...crig`）**真实网关为 SiliconFlow**（`https://api.siliconflow.cn/v1`，模型 `deepseek-ai/DeepSeek-V4-Flash`）；DeepSeek 官方 `api.deepseek.com` 返回 401。M5 UI 默认云端 Endpoint 已设为 SiliconFlow。
- **本地 Qwen**：模型在 `E:\AI_models\Qwen_Model\Qwen3-0.6B`，用同目录 `deploy`（GPU torch 2.13 cu132）venv 起 `serve_qwen3.py` 监听 `127.0.0.1:8001/v1`；`start_qwen.bat` 一键启动，与 PAI 文生图 8000 错开。该模型为 0.6B 小模型，**质量弱**，故默认仅作云端兜底。
- **后台进程跨 Bash 调用会被杀**：用 `nohup ... &` 起的 Gradio 服务在本次工具调用结束后即消失；要测服务端链路，需在**同一个脚本/同一次调用**内先 `launch(prevent_thread_lock=True)` 再跑 `gradio_client`（本环境已验证可行）。

---

## 7. 待解 / 开放问题

1. **M4 真 LLM 后端 → ✅ 已实装「云端+本地混合」并实测两端可用（2026-09-20）**：`OpenAICompatibleProvider`（stdlib `urllib`，零新依赖）+ `HybridProvider`，经离线双 stub 端到端验证（`TestLLMEndToEnd`×2 + `TestHybrid`×2 + `TestHybridCloudPreferred`×3 + `TestHybridValidator`×5）。M5 界面提供「仅云端（默认）/ 混合 / 规则 / 仅本地」四模式 + 云端/本地端点字段。
   - ✅ **云端 Key 已验证可用**：真实网关 **SiliconFlow**，NL→操作整链正确；UI 默认云端 Endpoint 已设为 SiliconFlow，填 Key 或设 `DEEPSEEK_API_KEY` 即生效。
   - ✅ **本地 Qwen 已实跑接入**：`serve_qwen3.py`（GPU/cu132）监听 `http://127.0.0.1:8001/v1`，`start_qwen.bat` 一键启停；实测 "去掉前2秒" 等指令可产出合法 JSON（偶发错答/空数组，**故默认云端优先、本地仅作离线兜底**）。
2. ~~**M2 FunASR 真实转写**~~：✅ **已完成**（2026-09-19）。
3. ~~**M5 编译 KeyError（2026-09-23）**~~：✅ **已修**，见第 8b 节；新增 `tests/test_robust_ops.py` 锁死回归。
4. **可选优化（非阻塞）**：M2 目前每次重新抽帧检测（关键帧 12 个 ≈1.5s，已够快）；若素材更长可加"按场景区间只抽代表帧"。

---

## 8. M5（Gradio UI）— 已完成（2026-09-19，2026-09-20 升级混合策略，2026-09-23 修实跑 bug + 接通真实出片）

**目标**：最小可用离线演示，架在 **RuleBasedPlanner + 确定性编译器** 上（无需 LLM Key 即可跑）；可选「云端优先+本地兜底」混合真 LLM。

界面（单页 `ai_video_agent/ui.py`，Gradio `gr.Blocks`）：
- 左栏：上传视频 → ①「摄取」显示 ffprobe 元数据 + 镜头区间 + 关键帧；②「跑视觉理解（M2·可选）」显示 YOLOv8 视觉段 + FunASR 转写 + **各段耗时**（依赖缺失优雅降级）；可折叠「真 LLM（可选）」选模式（**仅云端 SiliconFlow（默认·推荐）/ 混合：云端优先+本地兜底 / 规则（离线）/ 仅本地 Qwen**）+ 填云端/本地 endpoint+key+model。
- 右栏：③ 自然语言输入框（内置示例指令）+「生成操作」展示 `[{type,payload}]` JSON；④「应用并编译」→ **真正调用 ffmpeg 渲染成可预览/下载的 MP4（`gr.Video` 直接出成片，不再只是命令字符串）** + 结果 Timeline JSON + 可复制 FFmpeg 命令 + 字幕 ASS；「重置」清空状态。

关键实现点：
- 状态用 `gr.State` 持有 `FootageIndex / Timeline / ops`，多步编辑可叠加。
- 处理函数均为**模块级函数**，便于 `tests/test_ui.py` 直调验证（无需启动服务）。
- 可选真 LLM：`HybridProvider`（云端优先保正确，本地 Qwen 离线兜底；本地输出不可解析自动兜回云端）+ `OpenAICompatibleProvider`（标准库 `urllib`，零新依赖），选模式即热插拔进 `EditingAgent` → `JsonPlanner`；云端 Key 支持环境变量 `DEEPSEEK_API_KEY`。
- ④ 应用/编译走 `operations.apply_operations(strict=False)`：自动解析被 cut 拆掉的旧 id、单条失败只跳过并列出原因、操作把视频删光时直接判失败。
- **A/V 对齐铁律（2026-09-23 新增）：视频是主轨、音频跟随。** `cut` / `delete_clip` / `delete_range` 作用在视频上时，会同步作用于同 `sourceRef` 的配对音频；`trim` 按**素材区间**配对同步。反向不成立（删音轨不动画面）。
- **删除已检出内容用 `delete_range: {start, end}`**（素材绝对秒数）：执行器自己拆两刀再删中间，planner 不需要推演派生 id。多区间彼此独立、与顺序无关。
- **9 类操作全部真实渲染（2026-09-23 补齐，Bug M）**：编译器除 trim/拼接外，还会消费 `clip.effects`（画面/音频滤镜）、`tl.transitions`（边界淡出/淡入）、BGM 轨（`amix` 真混音）。`move_clip` 是**重排语义**（整轨重排为顺序相接），不是绝对定位。
- **编译器不再有"静默忽略"**：`compile()` 返回值带 `unsupported` 列表（未知效果名 / 转场时长过大 / 多视频轨合并等），④ 会把它显示成"⚠️ 以下内容不会体现在成片里"；`_apply_effect` 另有白名单校验，不认的效果名当场报错并转成可见警告。
- Gradio 6.x 适配：`gr.Code` 仅接受受限语言集（`json`/`shell`/`None`）；`demo.queue()` + M2 按钮 `show_progress="full"` 让长任务有进度条。

运行：
```bash
cd E:/多媒体融合剪辑/ai-video-agent
.venv/Scripts/python.exe -m ai_video_agent.ui     # 默认 http://127.0.0.1:7860
# 本地 Qwen（可选，离线兜底用）：双击 E:\AI_models\Qwen_Model\start_qwen.bat
```
验收：`测试素材.mp4` 全链路实测（Gradio 服务端）：① 摄取 2.6s → ② M2 首次 44.3s / 二次 4.2s（模型缓存生效）→ ③ 云端规划 → ④ **真实渲染出 MP4**。
- 「去掉前2秒」：成片 13.000s，video/audio 流均 13.000s。
- 「删掉有bus的片段 + 加字幕"开场"」（M2 事实 bus=0.50–4.00s / 10.50–14.00s）：成片 **8.00s**，`src_video` 与 `src_audio` 各 3 段 `(0,0.5)(4,10.5)(14,15)` **逐段镜像**；音频溯源曲线在 0.50s 处由源 0.5s 跳到源 4.00s（证明确实跟着剪）；字幕 1 条不重影。
- ④ 出片路径：`ai-video-agent/outputs/<时间戳>/output.mp4`（已在 `.gitignore` 忽略，不入库）。

---

## 8b. 2026-09-23 修复记录：M5 实跑踩到的 bug（均已修 + 回归测试）

用户实测截图反馈两处：① 点「② 跑视觉理解」像"没反应/跑不了"；② 点「④ 应用并编译」报 `KeyError: 'clip not found: src_video'`。

### Bug A — 编译失败：操作链引用了「已被 cut 拆掉的旧 id」（主因）
- **根因**：`_apply_cut` 把 `src_video` 拆成 `src_video__a`/`src_video__b` 后原 id 即消失；而 LLM/规则 planner 常继续写 `targetClipId: "src_video"`（提示词示例也都以 src_video 打头，模型会照抄），于是从第二条 op 起 `_find_clip` 抛裸 `KeyError`，**整批操作全废**。
- **修法（两层）**：
  1. `operations.apply_operations()` 新增**派生 id 别名表**：`cut` 后自动把旧 id 映射到当前存活的派生 id，并按 `at`/`in`/`out` 时间提示消歧（`src_video` → 按 at=8.0 解析为 `src_video__b`）；无法消歧时（如 delete 一个被切开的旧 id，语义上"删一段还是全删"不确定）**明确报错并列出候选**，不猜。
  2. `ui.do_apply_compile` 改走 `apply_operations(strict=False)`：单条失败只跳过并列出原因，其余照常生效；并加**安全闸**——若操作把视频片段全删光，直接判失败并说明原因，不产出无视频的空命令。
- **附带修**：`_make_agent` 的 hybrid 模式此前是 `prefer="local"`，与文档/界面标注的「云端优先+本地兜底」矛盾 → 改为 `prefer="cloud"`；本地未启动时混合模式自动退化为纯云端。

### Bug B — LLM 输出偶发无法解析：`Extra data: line 1 column 3`
- **根因**：模型偶尔返回带解释文字 / 代码块围栏 / 两段 JSON（实测过 `[]\n[{...}]`），`json.loads` 直接抛 `JSONDecodeError`。
- **修法**：`agent._extract_json_array()` 容错提取（剥围栏 → 直解 → 扫描第一个括号平衡的数组，跳过字符串内括号）+ 解析失败**自动重试一次**（附更强约束），仍失败则报错并回显两次原始输出，便于定位。

### Bug C — 规划器缺乏真实上下文（Bug A 的上游根因）
- 旧 `JsonPlanner` 只把 `timeline.dumps()` 给模型、**不给 M2 事实**，导致「删掉有bus的片段」被解释成「删掉整段 src_video」——实测一次直接产出 `delete_clip src_video` + `delete_clip src_audio`（把整条视频删光）。
- **修法**：提示词新增 ① **可用 clip id 清单**（`timeline_digest()`）② **M2 素材理解事实**（镜头 / 检出物体时间区间 / 台词，`facts_block()`）③ cut 派生命名规则 + **单区间与多区间两组 worked example**，并明确"时间永远是源视频绝对秒数，不是段内相对时间"（多区间场景正是模型最容易推歪 id 的地方）。
- **效果**：修前多区间删除实测 12 条 op 推歪（结果 `(0,0.5)(3.5,6.5)(6.5,15)`）；加多区间示例后**连测 3 次全对**（6 条 op → `(0,0.5)(4,10.5)(14,15)`）。

### Bug D — M2 点按体验（"第二步跑不了"）
- **排查结论**：`do_understand` 本体无 bug——用 `gradio_client` 连用户当时跑的实例实测 18–26s 正常返回（YOLOv8 出 bus/person/tie，FunASR 出完整中文转写）。
- **真实问题**：首次运行要加载 YOLO + Paraformer 权重（实测 44s，含一次性模型下载），期间**零反馈**；且 FunASR 默认联网做版本检查，弱网更久；未先点①就点②时提示也不够醒目。
- **修法**：① `understand.py` 加模型进程级缓存（`_YOLO_CACHE`/`_ASR_CACHE`，只加载一次）→ 二次点击 **44.3s → 4.2s**；② `AutoModel(disable_update=True, disable_pbar=True)` 去掉联网版本检查；③ 返回文案附各段耗时；④ `demo.queue()` + M2 按钮 `show_progress="full"`，长任务有进度条；⑤ 未摄取就点②时提示改为「请先点①」。

### 回归测试
- 新增 `tests/test_robust_ops.py`（19 用例）：stale id 解析（含多区间删除链）、歧义报错、未知 id 只跳过不中断、strict 模式、6 种 JSON 容错解析形态、重试一次、提示词含 id 与 M2 事实、**trim 的 A/V 配对同步（裁头/裁尾）**。
- 新增 `tests/test_render.py`（3 用例）：`resolve_ffmpeg()` 能定位 ffmpeg；**临时生成测试片源 → `render()` 真出 MP4 → ffprobe 校验时长 > 0**；ffmpeg 缺失时优雅返回 `(None, 原因)`。
**全量 163 passed + 27 subtests**（详见第 9 节的分文件明细；本文各节的历史数字已同步）。

### Bug E — 二次启动报 `OSError: Cannot find empty port in range: 7860-7860`（端口被旧实例占着）
- **现象**：关掉界面窗口后再双击 `一键启动界面.bat`，新窗口直接崩在 `launch()`，报找不到空闲端口。
- **根因**：关窗后 `python -m ai_video_agent.ui` 进程**未随之退出**，仍在后台 `LISTENING 7860`（实测残留意吃 1.5 GB，因已加载 YOLO+Paraformer 权重）；新实例再来抢同一端口即失败。
- **修法（双保险）**：
  1. **`一键启动界面.bat` 启动前自清理**：用 PowerShell 按命令行精确匹配 `*ai_video_agent.ui*` 杀掉本项目残留 UI 进程（**只动本项目 UI**，`photoshop-mcp-server` 等其它 python 服务不受影响），再等 1s 让端口释放。
  2. **`ui.py` 新增 `_pick_free_port()`**：7860 被占则自动顺延 7861…7879，并打印醒目提示；支持 `GRADIO_SERVER_PORT` 显式覆盖。即使清理失败也不会崩。
- **实测**：7860 被占时 `_pick_free_port(7860)` → `7861`（顺延生效）；清理后 7860 释放，新实例正常起，`GET /` 返回 **HTTP 200**，5 个端点（`do_ingest`/`do_understand`/`do_plan`/`do_apply_compile`/`do_reset`）全部就绪。
- **注意**：`_pick_free_port` 在 Windows 下**不设 `SO_REUSEADDR`**——Windows 语义会允许"抢占"已在监听的端口，反而制造冲突。

### Bug F — ④ 只返回命令字符串，不产出视频（用户反馈「返回这些代码很不可观」）
- **现象**：点「④ 应用并编译」后只看到 Timeline JSON / FFmpeg 命令文本 / 字幕 ASS，**没有任何成片**。
- **根因**：`compiler.compile()` 是纯函数，只生成 ffmpeg 命令字符串却**从不执行**（脚手架期设计如此，证明确定性契约）；UI 也没有 `gr.Video` 输出。
- **修法**：
  1. `compiler.compile()` 返回值新增 **`argv`**（结构化参数列表，`command` 字符串保持不变以兼容确定性测试），避免对命令串做空格切分（路径含空格会切错）。
  2. 新增 **`ai_video_agent/render.py`**：`resolve_ffmpeg()` 按「显式参数 → `FFMPEG_BIN` → 项目自带 `tools/ffmpeg/bin/ffmpeg[.exe]` → 系统 PATH」定位二进制；`render()` 用**按次时间戳子目录 + cwd** 运行（输出走相对名 `output.mp4`/`subs.ass`，规避 Windows 绝对路径在 filtergraph 里的 `:` 转义坑），返回成片绝对路径。
  3. `ui.do_apply_compile` 接 `render()`，新增第 6 个返回值（视频路径）；界面新增 **`gr.Video`「④ 成片预览/下载」** 组件并接线；④ 按钮加 `show_progress="full"`。
  4. 渲染"尽力而为"：ffmpeg 缺失/失败时状态里说明原因，但**不影响已成功的编译结果展示**。
- **实测**：`测试素材.mp4`（15s）跑「去掉前2秒」→ 真实产出 `outputs/<ts>/output.mp4`。

### Bug G — 真正执行 ffmpeg 后才暴露的编译/操作链缺陷（2 处）
接入真实出片后，第一次跑 ffmpeg 就暴露了两个此前"只生成命令、不执行"永远发现不了的 bug：
- **G1 单音频滤镜链拼接错误**：`compiler.py` 单音频分支产出 `...[ca0][ca0]anull[aout]`（标签重复 + 无意义的 `anull` + 缺分隔），ffmpeg 报 `Trailing garbage after a filter: anull[aout]`。
  → **修法**：单音频无需 `anull`/`amix`，直接 `af = aparts[0]`、`amap = alabels[0]`（复用其 `[ca0]` 标签）。
- **G2 trim 只裁视频不裁配对音频**：`_apply_trim` 只改目标 clip，同源音频 clip 仍是整段 → 「去掉前2秒」视频裁到 13s、音频仍 15s，混流后音频超前 2s、尾部定格。
  → **修法**：`_apply_trim` 增加**配对同步**——对「同 `sourceRef`、同初始时窗、不同轨」的 clip 施加同样的裁切与 `sourceIn/sourceOut` 平移，保持 A/V 对齐；并给 ffmpeg 命令加 `-shortest` 兜底，杜绝尾部定格。
- **实测**：「去掉前2秒」→ 成片 video 流 13.000s、audio 流 13.000s、总时长 13.000s（**A/V 完全对齐**）。
- ⚠️ **踩坑**：`Clip` 字段名是 `in_` / `out`（不对称，`out` 无下划线），写同步逻辑时误写成 `o.out_` 导致尾部不同步——单测当场抓出。

### Bug H — 字幕显示成 `\an2开场`（ASS 覆盖标签缺 `{}`）
- **现象**：出片后画面里字幕文字变成 `\an2开场`，把对齐标签当正文显示了。
- **根因**：`compiler.build_ass_content()` 把对齐标签拼成 `{an}{text}` = `\an2开场`——ASS 里覆盖标签必须用 `{}` 包裹（`{\an2}`），裸写会被当普通文字。
- **修法**：改为 `{{{an}}}{text}` → `{\an2}开场`；`tests/test_contract.py` 断言加严为 `assertIn("{\\an2}你好", ass)`。
- **实测**：抽帧确认字幕正确显示为「开场」且底部居中。

### Bug I — ④ 输出整批报错：Gradio 位置契约错位（`gr.Video` 收到一段 JSON）
- **现象**：点④后，界面右侧 ④ 那一整块（状态/成片/JSON/命令/字幕 5 格）全部渲染成"错误"卡，卡内无任何原因文字。后端其实是**成功的**——`outputs/<ts>/output.mp4` 真实生成（595KB）。
- **根因**：**位置契约错位**。`btn_apply.click(outputs=[...])` 是位置契约，而我上轮加 `video_out` 时把它插在 outputs 列表**第 3 位**，却把 `video_path` 追加在返回元组**末尾**，于是整批后移一格：

  | 位 | 返回元组实际 | 接的输出组件 |
  |---|---|---|
  | 3 | `tl.dumps()`（JSON） | `gr.Video` ← 被当成文件路径 |
  | 4 | `result["command"]` | `gr.Code`（tl_json） |
  | 5 | `ass` | `gr.Code`（cmd_box） |
  | 6 | `video_path` | `gr.Code`（ass_box） |

  服务端 `gr.Video` 处理这段"路径"时抛异常 → **Gradio 里一个输出的后处理失败会让该依赖的 *全部* 输出显示为错误卡**（实测 `gradio_client` 直接收到 `AppError`），这就是"5 张卡"的来源。
- **修法**：
  1. `ui.py` 新增唯一顺序常量 `APPLY_OUTPUT_KEYS`，`do_apply_compile` 内部用 `out(**kw)` 按它摊平成元组——**顺序只有一个来源，手写元组字面量被彻底消除**；
  2. `build_app()` 的 outputs 列表旁加注释指向该常量；
  3. 测试改为按契约字典取值（`_apply()`），不再写死位置。
- **防回归**（`tests/test_ui.py::TestUIOutputContract`，3 用例）：
  - `test_apply_output_order_matches_component_types`：用 `build_app().fns` 取出**真实接线**的组件列表，按类型逐位校验（第 3 位必须是 `gr.Video`、第 4/5/6 位必须是 `gr.Code`…）；
  - `test_apply_values_land_in_the_right_slots`：按**内容**校验归位——视频格只能是路径/空串（不能以 `{` 开头）、第 4 格必须是合法 JSON、第 5 格必须含 `ffmpeg`、第 6 格不能含 `ffmpeg`；
  - `test_all_click_outputs_arity_matches_return_annotation`：通用兜底，每个处理函数接的输出数 == 返回类型标注的元素个数。
  - ✅ **已验证该测试真能抓住此 bug**：把返回临时改回旧写法后，`test_apply_values_land_in_the_right_slots` 当场失败并打印 `video_out 收到了 JSON：'{\n  "metadata"...'`。
- **连带改进**：`launch(..., show_error=True)`——出错时界面直接显示真实异常，不再是"一张没有信息的错误卡"（本次就因为卡里没文字，多花了一轮排查）。**对外演示若不想露堆栈，改回 `False` 即可。**
- **实测**：真服务 + `gradio_client` 全链路（`测试素材.mp4`）：① 摄取 → ③ 规划（`去掉前2秒`）→ ④ 编译，视频格拿到真实文件路径、文件存在、JSON/命令/字幕各归其位；成片 `13.000s`（video 流 13.000 / audio 流 13.000，A/V 对齐）；加字幕那一路 `ass` 格内容正确。

### Bug J — 「有 bus 的部分没删掉」：多区间删除被**静默跳过**（只生效一半）
- **现象**（用户反馈 + 附件成片）：指令 `删掉有bus的片段`，成片却**仍有 bus**；用户感觉"输出和提示词有差别"。
- **逆向取证**（先测量再下结论）：对用户成片做「指纹比对」——
  - `ffprobe`：时长 `11.52s`（源 15s，**只少了 3.48s**）；
  - `silencedetect`：成片静音位置**与源素材逐毫秒一致**（2.176833 / 4.573375 / 7.347875）→ 音频没被剪过；
  - 逐帧静止段结构：成片 `[(8,111),(134,110),(258,24)]` vs 源 `[(8,111),(134,110),(258,111)]`
    → **前两段完全一致**，第三段被截短 ⇒ **成片 = 源 `[0,10.5s] ++ [14,15s]`**。
  - ⚠️ 中途差点误判：源素材本身就是**3 张静态图各停 ~4.4s** 的幻灯片，"相邻帧完全相同"是**源的正常特征**，不是渲染冻结。**先量源，再下结论。**
- **根因**：M2 事实是 `bus 0.50–4.00s` 与 `10.50–14.00s` 两个区间。旧提示词要求模型「切两刀 + 删中间」并**自己推演派生 id**（`src_video__b__b__b__a`）。第一组三步执行后 id 全变了，第二组引用的 `src_video__b` 已指向 `[14,15]`，被非严格模式**静默跳过**：
  ```
  4. `cut` → 已应用（`src_video` 已被切分，按时间提示自动解析为 `src_video__a`）
  5. `cut` 已跳过：cut at=4.0 必须严格落在 (14.0,15.0) 开区间
  6. `delete_clip` 已跳过：片段 `src_video__b__a` 不存在…
  ```
  ⇒ 只删掉第二段 bus，第一段原样保留。**"跳过式容错"在这里等于静默的数据丢失。**
- **修法**：新增**免 id 的范围型操作 `delete_range: {start, end}`**（按素材绝对秒数删除）。
  - 执行器内部自己拆两刀再删中间，模型只填业务坐标，**多区间彼此独立、与顺序无关**；
  - 提示词删掉"按上一步推演 id"的 worked example，改为每个区间一条 `delete_range`；
  - `RuleBasedPlanner._remove_range` 同步改为单条 `delete_range`；
  - `delete_range` 命中不到任何 clip 时**抛错告警**（而不是静默成功）。
- **实测（修复后）**：同一指令 → 成片 `8.00s`，`src_video` 与 `src_audio` 各 3 段 `(0,0.5)(4,10.5)(14,15)` **逐段镜像**。全量 **163 passed**。

### Bug K — 音频不跟着视频剪辑走（A/V 失步）
- **用户提问**：「删除前两秒，音频不也会被删除掉的吗」——**旧版本答案是"不会"**。
- **根因**：`src_audio` 是独立 clip，只有 `trim` 做了配对同步，`cut` / `delete_clip` **完全没管音频**。
  视频剪掉一段后，音频仍在原时间轴播放；`-shortest` 只是把音频**按视频长度截断**，并不会让它跟着剪辑走。
  实测：成片视频是 `源[0,10.5]++源[14,15]`，而音频是 `源[0,11.52]` 连续原声 ⇒ **从 10.5s 起音画即错位**。
- **修法**（确立 **"视频是主轨、音频跟随"** 铁律）：
  1. `cut`：同一素材时刻把配对轨（同 `sourceRef`、其它 track）一起切开；
  2. `delete_clip`：删掉视频段时，按**素材区间**从配对轨上摘掉同一段（反向不成立——删音轨不会动画面）；
  3. 抽出通用基础设施 `_split_at_source` / `_piece` / `_remove_source_range`，`delete_range` 复用；
  4. `_apply_trim` 的配对判据从"时间轴 `in_/out` 相等"改为"**素材区间相等**"，cut 之后依然能配对（此前会失配）；
  5. **编译器修正**：多段音频原先用 `amix`（并行混音，总长只取最长段）→ 改为 `concat=n=K:v=0:a=1[aout]`（顺序拼接）。
- **实测（修复后）**：`src_video__a/b__a/b__b` 与 `src_audio__a/b__a/b__b` **素材区间完全一致**；
  音频溯源曲线在 **0.50s 处由源 0.5s 跳到源 4.00s**（旧版本是一条直线）⇒ 音频确实跟着剪了。全量 **163 passed**。
- **洞察**：`ffprobe` 显示"视频流与音频流时长相等"**并不能证明 A/V 同步**——`-shortest` 会让错位的成片看起来完全正常。必须做**内容溯源**。

### Bug L — 字幕重影（同一条字幕被画两遍）
- **现象**：成片里「开场」明显加粗/重影；`subs.ass` 里有**两条完全相同的 `Dialogue`**。
- **根因**：LLM 偶发把同一条 `add_subtitle` 输出两遍；`_apply_subtitle` 允许重叠（v1 契约），于是叠加渲染。
- **修法**：`apply_operations` 对 `add_subtitle` 做去重（文本 + 时间窗 + 位置 + 轨道 全同才去重），并记入警告说明"叠加会变成重影"。
- **实测**：同一 ops 输入 → 字幕 **1 条**，抽帧确认单层渲染。

---

### Bug M — 4 类操作"改得了时间轴、改不了成片"（空转，静默失效）
- **发现方式**：用户问"基本功能齐全了吗"，于是不读代码、直接做**出片矩阵验证** —— 对契约里 9 类操作各构造一条指令，走完整链路真渲染，再用 ffprobe / 像素 / PCM 差分判定"成片到底变了没有"。
- **现象**：`add_bgm` / `move_clip` / `transition` / `effect` 四类操作**都能成功改 Timeline、界面显示"✅ 已应用并编译"，成片却与不加一模一样**。
- **硬证据**：加 BGM 前后 **PCM 平均绝对差 = 0.00**（音频流逐样本相同）；`effect(grayscale)` 后 t=1.0s 像素仍是 `(1,0,255)`；`transition(fade)` 后边界像素仍是 `(255,0,0)`；`move_clip` 调序后片段顺序仍是 `[__a, __b]`。
- **根因**：`compiler.py` **从不消费** `clip.effects` / `tl.transitions` / BGM 这三块数据 —— 它只用了 `sourceIn/sourceOut`（trim）与视频拼接顺序。契约层收下了操作，渲染层却没人读。
- **为什么 75 个测试全绿没拦住**：operation 侧只断言"Timeline 字段被改了"，compiler 侧只断言"命令字符串里有某个片段"，**没有任何一条断言"操作改变了成片"**。这是"只测中间态、不测最终产物"的典型盲区。
- **修法（4 项一起补）**：
  1. `effect` → 逐片段注入滤镜链（另补 `fade_in`/`fade_out`：此前「淡入」会被效果白名单拒绝，而它是最常见的画面效果请求）：`grayscale→hue=s=0`、`blur→boxblur`、`brightness/contrast/saturation→eq`、`sepia/invert/mirror`、`fade_in/fade_out`（单个片段也能淡入淡出，不必先切两刀）等；音轨支持 `volume/fade_in/fade_out`。
  2. `add_bgm` → BGM 移到独立 `a_bgm` 轨（不再与主音频混在同轨），编译器单独抽出做 `amix=inputs=2:duration=first:normalize=0` 真混音（`normalize=0` 保证主音不被压低、`duration=first` 保证长度跟视频走）。支持 `volume/fadeIn/fadeOut/adelay`。
  3. `transition` → 在片段边界注入 `fade=t=out` / `fade=t=in`（淡出到黑/白再淡入；不做交叉溶解，如实说明）。
  4. `move_clip` → 改为**重排（reflow）语义**：把目标片段插到新位置后整轨重排为首尾相接，顺序变化才真能体现在成片里（只改自身 `in_` 会与邻段撞车、退化成原顺序）。
- **顺带堵住"新的静默"**：`compile()` 返回值新增 **`unsupported`** 列表，识别不了/表达不了的东西（未知效果名、转场时长超过片段、多视频轨合并）一律列出，由 ④ 回显；`_apply_effect` 对效果名做**白名单校验**，不认的当场报错 → 非严格模式下变成一条可见警告，而不是默默什么都不做。
- **回归护栏**：新增 `tests/test_render_ops.py`，其中 `test_every_op_changes_compiled_output` 对 `OPS_V1` 里**每一个**操作断言"应用后编译产物必须变化" —— 以后新加操作若编译器没消费它，这条直接失败。
- **实测（真实素材 `测试素材.mp4`，15s）**：

  | 操作 | 判据 | 结果 |
  |------|------|------|
  | 黑白 | 8 个采样点彩度 | 原片最大彩度 **19 → 0**（全帧去色） |
  | 淡入淡出 | 转场点像素 | `(56,56,59)` → **`(0,0,0)`**（边界变黑） |
  | 背景音乐 | 音频 RMS | **3202.3 → 3311.3**（真混进去了） |
  | 片段重排 | 首帧像素 | `(56,56,59)` → **`(16,16,16)`**（画面顺序变了） |

  合成素材上的极限判据：灰度使蓝色 `(1,0,255)` → `(31,28,31)`（R≈G≈B）；重排使首帧由蓝变红；BGM 使 PCM 平均绝对差达 **1301.97**。

---

### Bug N — 语义删除只删「关键帧命中跨度」，不删「整个镜头」（残留画面 + 零长度区间）

- **发现方式**：用户要求"再生成两段测试素材"。新素材2 故意做成 **3 个公交镜头**（24s 六幕，公交在 0–4 / 8–12 / 16–20s）。同一句 `删掉有bus的片段` 跑出来是
  `delete_range(2.0, 2.0)`、`(8.0, 10.0)`、`(16.0, 18.0)` —— 第一条是**零长度区间**：
  `delete_range` 要求 `start < end`，严格模式直接抛错，而 UI 走 `strict=False` 时被**静默跳过**（又一次"悄悄少删"）。
- **根因**：`understand.detect_objects` 把 YOLO 在各关键帧上的同标签命中用 `_merge_times` 合并成区间，粒度是**关键帧**而不是**镜头**：
  1. 只被一个关键帧命中的镜头 → 合并出零长度区间 `(t, t)`；
  2. 即便命中多个关键帧，区间也**盖不满整个镜头**。
- **回溯**：这正好解释了 Bug J 修复后成片为何是 `源[0,0.5]++[4,10.5]++[14,15]` —— 源里 `0–5s` 的公交镜头只在 `0.5/2/3/4s` 命中关键帧，
  区间 `(0.5, 4.0)` 删完**仍留着 0–0.5s 与 4–5s 的公交画面**。用户最初那句"有bus的部分都没有删除掉"，到这里才彻底解决。
- **修复**：`understand.py` 新增 `_scene_index_at` / `_label_ranges_from_times`，把「命中关键帧」外扩成「命中镜头」：
  落在镜头边界上的关键帧归**下一镜**（与 `ffmpeg -ss` 的抽帧语义一致），相邻命中镜头合并为一段；无镜头信息时退化为关键帧跨度但**保证非零长度**。
- **实测（真实素材，修复后）**：

  | 素材 | 规划区间 | 成片 | 残留公交画面 |
  |---|---|---|---|
  | `测试素材.mp4`（15s，公交镜头 0–5 / 10–15s） | `(0,5)` `(10,15)` | **5.00s**（旧版 8.00s） | 无 |
  | `测试素材2_多目标.mp4`（24s，公交镜头 0–4 / 8–12 / 16–20s） | 3 条，各覆盖整镜 | **12.00s** | 无；A/V 逐段镜像，0 警告 |

- **回归护栏**：`tests/test_agent.py::TestSemanticRemoveCoversWholeShot` ×7 —— 覆盖「单关键帧命中必须外扩成整镜」「边界时刻归下一镜」「无镜头信息仍非零长度」，
  并端到端断言**保留区间与任何公交镜头都不得有交集**（这正是"删干净"的可机器判定形式）。

### Bug O — 竖屏/小分辨率下字幕横向溢出被画面裁掉（ASS 没有声明坐标系）

- **发现方式**：新素材3（720×1280 竖屏）跑「加字幕」后**抽帧用眼睛看** —— 6 个汉字撑满整个 720px 宽，
  两端各被裁掉约一个字。横屏 1280×720 上同一份代码看起来完全正常，所以此前一直没暴露。
- **根因**：`build_ass_content` 产出的 ASS **没有 `PlayResX/PlayResY`**。libass 缺省按 **384×288**
  解释脚本坐标，于是声明的 `Fontsize: 36` 实际含义是"画面**高度**的 12.5%"、与画面宽度无关：
  1280×720 → 有效字号约 90px（装得下）；720×1280 → 有效字号约 160px，6 个字约 960px > 720px → 溢出被裁。
  另外 `WrapStyle: 2` 表示"只按 `\N` 断行"，长字幕不折行、一律溢出。
  还有一处脆弱点：Style 的 `Format` 行少了 3 个颜色/编码字段，并把 `Outline`/`Shadow` 各声明两次
  （第 5、6 个字段填的其实是颜色值）——解析结果依赖 libass 对重复字段的处理细节。
- **修复**：显式声明 `PlayResX/PlayResY = timeline 分辨率`、`WrapStyle: 0`（允许自动折行）、
  `ScaledBorderAndShadow: yes`；改用**标准 V4+ 23 字段**的 Style Format；
  字号取 `min(H×4.8%, W×5.5%)`（同时受宽高两维约束），描边/阴影/边距按同比例缩放。
- **实测**：竖屏抽帧 6 个字完整落在画面内（见 `outputs/验证_竖屏字幕.png`）；
  横屏字号同步收敛到合理值（此前约 90px 明显偏大）。所有带字幕的样片已按新样式重出。
- **回归护栏**：`tests/test_contract.py::TestSubtitleFitsFrame` ×5 —— 核心是
  `test_playres_matches_resolution`（旧实现必然失败）与 `test_text_fits_width`（在钉死的坐标系下估算文本宽度）。
  **这个 bug 的教训：光看声明的字号看不出来（36 看着很小），必须先把坐标系钉死。**

### Bug P — 纯音乐素材被 FunASR **幻觉**出一整片段英文（污染事实块与界面）

- **发现方式**：新素材3（**纯音乐、无任何语音**）跑 M2 转写 → 返回 `0.00–20.00s "the"`。
  一个横跨全片、只有一个英文单词的"台词"，界面与 LLM 事实块照单全收。
- **根因**：FunASR 对无语音音频会幻觉出短英文词；又因为**退化分支取整片时长**
  （`sentence_info` 缺失时 `start=0.0, end=metadata["duration"]`），幻觉被放大成"整片都在说 the"。
- **顺带查清的两件事（实测，别再重复试）**：
  1. 当前 FunASR 配置**拿不到句子级时间戳**。`generate()` 返回体只有 `{"key","text"}` 两个键 ——
     **没有 `sentence_info`**，`sentence_timestamp=True` 传了不生效，返回体也不含 `timestamp`。
     实测同一条旁白只能拿到**整片一条**、且识别文本**幕序错乱**
     （"…第四幕…第六幕…第五幕…"）。要**句级**断句需挂 `punc_model`（数百 MB），暂不引入；
     但**段落级**时间戳已经拿到了 —— 见下面的修复第二段。
  2. **把 `vad_model="fsmn-vad"` 挂在 `AutoModel` 里不解决问题**：实测仍是 1 段、仍无 `timestamp`、
     **音乐幻觉依旧**（纯音乐仍返回 `'the'`）。原因是它只是内部切段后又把结果拼成一条返回，
     **段边界被丢掉**；且 **VAD 会把纯音乐整段判成语音**（实测素材3 → `[[0, 19980]]` 覆盖全片），
     这才是"挂了 VAD 也治不了幻觉"的根因。（权重落在 `D:\Dev\Cache\modelscope`，非 C 盘。）
- **修复（三段）**：
  1. **幻觉护栏（单段）** `understand._looks_like_hallucination()` —— 只在**结构上不可能成立**时才丢：
     同时满足「覆盖 ≥90% 时长」+「不含汉字」+「词数 ≤ 2」。`'the'` / `'Thank you'` 被丢弃，
     而素材2 那种含汉字、字数多的**中文**旁白**不会**被误杀。带真实断句（`sentence_info`）的段一律保留。
  2. **VAD 分段转写**（`_get_vad` / `_vad_spans` / `_slice_wav`）—— 关键认知是
     **单独实例化 VAD**（而不是挂进 `AutoModel`）才能拿到原始段边界 `[[起ms, 止ms], ...]`；
     拿到后**逐段切音频、逐段转写**，于是每段都带时间戳，**文本顺序也天然正确**。
  3. **静音阈值 800 → 300ms** + **整体幻觉判据** `_looks_like_silent_footage`。两者必须同时做，理由见下。
- **实测（素材2，六幕旁白）**：粒度从「整片一条 + 幕序错乱」→ 默认 800ms 的 **3 段** → **300ms 的 11 段**，
  段边界落在 `4150/8150/12140/16160/20160`，**与幕边界 4/8/12/16/20s 精确吻合**（差值即旁白起始的 0.25s 延时）：
  `[0.08-1.28] 第一幕` / `[1.28-3.35] 画面里有一辆公交车` / `[4.15-5.27] 第二幕` / …
- **调细阈值的反噬（很重要）**：同一改动让**纯音乐素材从 1 段变成 6 段**，逐段幻觉出
  `'嗯嗯' / '呜啦啦' / '呜呜呜' / '嗯' …`。此时**单段判据全部漏过** —— 每段只覆盖 16% 时长
  （`covers_all` 不成立），文本又含汉字（`has_cjk` 反而成了"像真台词"的证据）。
  → 新增**整体判据**：段数 ≥3 + 每段 ≤3 字 + 区间并集覆盖 ≥90% 时长 → 整批丢弃。实测素材3 恢复为**空**。
- **参数怎么传（踩过一次）**：VAD 参数是**直接作为关键字**传给 `AutoModel` 的（`VADXOptions(**kwargs)` 消费）。
  签名里那个 `vad_post_args` **从未被使用** —— 第一次用它传参，四个不同阈值跑出**完全相同**的结果
  （静默无效），差点得出"静音阈值不影响分段"的错误结论。
- **取舍（如实标注）**：① 素材里若真的只有一句极短英文台词（如 `"Go!"`）或整片只有 1–3 字短句，会被丢；
  ② 转写段时间戳为**段落级**。影响可控 —— 转写段只进事实块与展示，**实际删除用的是镜头区间**（见下）。
- **实测（护栏）**：素材2 → 11 段且与幕边界对齐；素材3 → **转写段为空**。复跑 `_verify/verify_asr_guard.py`。

### 新增能力 — 按台词删除（`_speech_ranges` + 规划器分支）

有了带时间戳的转写段后，规划器支持「删掉**说** X 的片段」这类指令。

- **关键设计：台词区间同样外扩到镜头。** 台词粒度可到"半句"（`[8.15,9.34] "第三幕"`），
  直接删会只删半句、留下半个镜头 —— 与 Bug N 是同一条铁律。`_speech_ranges` 把它扩成覆盖的镜头 `(8.0,12.0)`。
- **事实块同步改造**：台词**按镜头聚合**后再交给 LLM
  （`镜头 8.00-12.00s: "第三幕" / "又出现了一辆公交车"`），模型拿到的是**可直接使用的删除区间**，
  不需要自己推演外扩（推演极易出错）。
- **视觉优先、台词兜底**：「删掉**有**X的片段」先查视觉标签，视觉无匹配再回退台词检索
  （解决"用户说中文『公交车』、视觉标签却是英文 bus"的错位）。
- **交叉验证**（素材2，24s）：「删掉**说**公交车的片段」（台词路径）与「删掉**有**公交车的片段」
  （视觉路径）给出**完全相同**的 `[(0,4),(8,12),(16,20)]`；成片均 **12.00s**、
  video=12.000000 / audio=12.000000、**0 警告**。复跑 `_verify/verify_speech_delete.py`。
- **性能：分段的代价，以及怎么压回去**。分段把 1 次转写变成 N 次，A/B 实测整片一次 **1.38s**、
  VAD 分段 **5.03s**（3.64×）。但拆开计时才看出关键：**5.03s 里转写只占 1.72s，
  剩下 3.30s 全花在 11 次 ffmpeg 切音频的进程开销上** —— 优化重点不在模型，在切段方式。
  改为**一次读 wav 成 numpy → 按段切片 → 批量 `generate(input=[...])`** 后降到 **1.36s（3.7×）**，
  且文本逐条完全一致（"一致"必须显式逐条比对 —— dtype/取值范围不对时模型可能**静默转出垃圾**）。
  两级兜底：soundfile 不可用 → 退回 ffmpeg 切段；**批量条数 ≠ 输入条数 → 退回逐条**
  （否则时间戳会错位到别的段，比慢一点严重得多）。
- **回归护栏**：`tests/test_understand.py::TestAsrHallucinationGuard` ×7 + `::TestVadSpans` ×7 +
  `::TestSilentFootageGuard` ×7（整体判据）+ `::TestAsrSegmentTexts` ×5（转写两级兜底）+
  `tests/test_agent.py::TestDeleteBySpeech` ×14 + `tests/test_ui.py` 的 UI 路径用例 ×3。
  性能复跑：`_verify/bench_transcribe.py` / `bench_batch_asr.py` / `bench_numpy_slice.py`。

### Bug Q — 提示词与事实块**自相矛盾**：退化分支没自报家门，且提示词内容**从未被测试**

- **发现方式**：按台词删除上线后回头审提示词契约，发现两件事同时成立 ——
  ① `facts_block` 在**拿不到镜头信息**时会退化成逐条列台词（没有「镜头 X-Y」字样），
  但 schema 规则 3b **无条件**写着"事实里台词已经**按镜头聚合**，每条前面的「镜头 X-Y」就是删除区间"；
  ② 提示词内容**没有任何断言** —— stub 测试只校验了 model 名 / messages 非空 / 鉴权头，
  模型返回什么由 stub 自己造。**所以事实块或规则一旦被重构改坏，所有测试照样全绿，LLM 路径悄悄退化。**
- **后果**：提示词在对模型**说假话**。模型会去找不存在的「镜头 X-Y」，或者违反 3b 改用台词小时间戳去删
  —— 又回到"只删半句、留下半个镜头"（Bug N 的同一类症状），而且**不报错**。
- **修复（三处一起改）**：
  1. **退化分支自报家门**：`facts_block` 的退化输出改标注
     `台词（**未按镜头聚合：没有可用的镜头信息，下面的台词区间就是能给的**）`；
     正常分支保持 `台词（**按镜头聚合：…**）`。两条路径的**首个标识**不同，模型一眼能分辨。
  2. **规则 3b 改成条件式**（先看开头标识再决定用哪份数据）+ 补一个**按台词删除的 worked example**
     （示例里明确写"要用「镜头 X-Y」，不是台词自己的小时间戳"）。
  3. **补提示词契约测试**：拼装层断言 `build_prompt()` 返回值的**事实段**里有聚合区间；
     **线上层**让 stub 存下服务端实际收到的请求体，断言里面真的有聚合区间/台词原文/指令/命名约定/id 清单。
- **顺带补的降级回显**：同一件事在**规则路径**上没有模型兜底 —— 没有镜头信息时
  `_speech_ranges` 只能退化成语音段落那 1–2s，成片必然残留半个镜头。
  新增纯判定函数 `speech_delete_degrades_to_raw_segments(cmd, index)`，`do_plan` 的状态里
  显式提示"本次没有可用的镜头信息，删除区间只能取语音段落"。判定用的正则与规划器**共用同一常量**
  `_SPEECH_DELETE_RE`，避免两处各写一份日后分家。
- **两个测试写法上的坑（已写进测试注释）**：
  - **子串断言会自欺**：`"未按镜头聚合"` **包含** `"按镜头聚合"`，只 `assertIn("按镜头聚合")`
    会在退化场景**假通过** → 断言带前缀（`"台词（**按镜头聚合"`）并补 `assertNotIn` 反向标识。
  - **对整条提示词 `assertNotIn` 必然误报**：schema 的【示例】区天然含有与事实区同形的字符串
    （示例就该长得像真的）→ 断言必须先按标记**切出事实段**再判。
- **回归护栏**：`tests/test_robust_ops.py::TestSpeechPromptContract` ×4（含一条
  `test_no_inconsistency_between_hint_and_facts` 交叉断言，防提示词与事实块两处各自演化而分家）+
  `tests/test_agent.py` 事实块退化/降级判定 ×6 + `tests/test_ui.py` 线上层契约与降级回显 ×3。
- **实测复跑**：`_verify/verify_speech_delete.py` 结果与修复前**完全一致**
  （素材2 转写 11 段、两条路径同为 `[(0,4),(8,12),(16,20)]`、成片 12.00s、0 警告）。

### Bug R — 真机验证：模型**少删/多删**且不报错（→ 新增「确定性事实核对」闸）

- **怎么发现的**：用**本地真实模型**（Qwen3-0.6B @ 8001，`E:\AI_models\Qwen_Model\start_qwen.bat`）
  第一次把 LLM 路径跑通 `_verify/verify_llm_real.py` —— 之前这条路径只有 stub 验证过。结果：

  | 指令 | 模型实际输出 | 后果 |
  |---|---|---|
  | 删掉**说**公交车的片段 | 只给 **2 条** `(0,4)` `(8,12)` | **少删**：第 3 个公交镜头 16–20s 仍在成片里 |
  | 删掉**有**公交车的片段 | 给 **4 条**，多的那条是 `(20,24)` | **多删**：把用户没要的第 6 幕也删了（成片 8s 而非 12s） |
  | 去掉前2秒 | `(trim, in=2.0)` | ✅ 正确 |

  **两次错误都是「操作成功、0 警告」** —— 时间轴确实改了、成片也出得来，但结果和指令对不上，没人吱声。
  这是本项目最危险的一类失效（此前所有"自动重试/容错解析/别名表"都救不了，因为模型给的是**合法 JSON**）。
- **好消息**：模型**没有**用台词小时间戳（`8.15-9.34`）——规则 3b + 聚合事实块在真机上确实生效了；
  且输出总带 ` ```json ` 围栏，`_extract_json_array` 的围栏剥离是必需的（被真机验证覆盖到了）。
- **根因**：语义删除（"删掉有/说 X 的片段"）本质是**检索**而不是创作 —— 答案已经写在 YOLO 标签和
  ASR 转写里了。既然手上有标准答案，就不该只靠"提示词写得够清楚"去赌模型照做。
  （`temperature=0` 下两次跑出**完全相同**的输出 → 这是稳定偏差，不是偶发抖动。）
- **修复：新增「确定性事实核对」闸**（`agent.semantic_delete_discrepancies`）：
  - `expected_semantic_ranges(cmd, index)`：从 M2 事实**确定性**算出"这条指令该删哪些区间"
    （语义删除走视觉优先 / 台词兜底，按台词删除走台词检索 —— 与规则规划器共用同一套正则）；
  - 核对模型输出，报出 **漏删**（期望里有、模型没给）与 **多删**（模型给了、期望里没有），容差 0.05s 吸收舍入噪声；
  - **`do_plan` 把这三条挂到状态里**，并提示"建议切到规则 Planner 得到精确定位的结果"。
  - **自动纠正（2026-09-24 收口修复）**：发现不一致且 `expected_semantic_ranges()` 能算出确定性标准答案时，
    `do_plan` 会**直接把模型不一致的删除区间替换成规则 Planner 的精确结果**，保证交付物正确；
    同时把"漏删 / 多删"原样摆给用户看（透明，不是静默替换）。仅在算不出标准答案
    （没有 M2 事实 / 关键词从未命中）时才保持"只警告"，因为那种情况下没有可纠正的真相。
- **补钉：这道闸自己的死角（2026-09-24 同一轮追加修正）**。第一版在"`expected` 算不出来"时
  直接返回 `[]`，于是出现两个失效态 —— 实测复现如下：

  | 场景 | 第一版行为 | 现在 |
  |---|---|---|
  | 已摄取但**没跑 ② M2**（无镜头/标签/台词） | 报 N 条「多删」——**误导**，真因不是区间算错，是压根没有事实 | 单条明确提示：**没有可用的 M2 结果，模型多半是臆测，请先跑 ② 素材理解** |
  | `index` 为 None | **0 条警告，完全不核对** —— 和没有这道闸一样 | 同上，一样会提示 |

  > 一条通用教训：**校验器在"算不出来"时保持沉默，等价于没有校验器。**
  > 数据缺失本身就是一种必须报出来的状态，而且要报真正的原因（缺数据），
  > 而不是把它伪装成另一种错误（多删）。
  相应地，有事实但关键词一个都没命中时，现在说「素材里没有任何内容命中『X』」，
  而不是逐条列「多删」。
- **实测 ①（`_verify/verify_llm_real.py`，同一份真机输出）**：三处不一致**全部被拦下**
  （漏删 16.00-20.00s / 多删 20.00-24.00s），非语义指令（`去掉前2秒`）**无误报**。
- **实测 ②（`_verify/verify_ui_llm_real.py`，真模型 → 真 App 路径 → **真出片**）**：
  把"用户最终拿到什么"摆出来 —— 走 `do_plan` → `do_apply_compile` → 渲染 → ffprobe：

  | 后端 | 规划区间 | **成片** | ③ 状态 |
  |---|---|---|---|
  | 真模型 Qwen3-0.6B | 模型给 4 条（含多出 `20-24`）→ **自动纠正为 3 条** | **12.00s**（正确） | ⚠️ **核对提示出现 + 已自动改用规则结果** |
  | 规则 Planner | 3 条 | **12.00s**（正确） | 正常 |

  → 修复前差 4.00s（拿到错的 8 秒成片）；**修复后 `do_plan` 在规划阶段就把区间自动纠正，
  真模型路径与规则版同为 12.00s、差 0.00s、一致 ✅**。"只警告 vs 自动纠正"的取舍已落定为
  "有标准答案就自动纠正、无标准答案才只警告"，保证交付物永远正确。
- **回归护栏**：`tests/test_agent.py::TestDeleteBySpeech` ×27（含期望值、容错容差、畸形 ops、
  **无 M2 事实 / index 为 None / 关键词未命中**三个死角）+ `tests/test_ui.py::TestSemanticDeleteGuard` ×4
  （走 **UI 全链路**，用 stub 回放**真机那份原始输出**，断言状态里出现"删除区间与素材事实不一致"
  **且 `ops` 已被自动纠正为正确区间**；以及"没跑 M2"场景指向第 ② 步）。
- **顺带修掉一个假绿测试**：`test_discrepancy_check_handles_malformed_ops` 原本只喂畸形 op，
  守卫会在"没有可解析 delete_range"时提前返回，`all(...)` 作用在**空列表**上恒真 ——
  测试永远通过却什么也没检查。现改为混入一条合法 op 并断言**具体条数**。
- **产品决策已落定（2026-09-24 修复）**：发现不一致且能算出确定性标准答案时**自动改用规则结果**
  （`do_plan` 里把模型不一致的 `delete_range` 替换为 `expected_semantic_ranges()` 算出的精确区间，
  非删除类操作原样保留）。这样既保证交付物正确，又通过状态文案把"哪里不对"透明摆出，不是静默替换。
  仅"无 M2 事实 / 关键词从未命中"这类算不出标准答案的情况仍保持"只警告"。
## 9. 如何运行 / 验证

```bash
cd E:/多媒体融合剪辑/ai-video-agent
.venv/Scripts/python.exe -m pytest tests/ -q      # 期望 163 passed + 27 subtests
# 单跑某层：
.venv/Scripts/python.exe -m pytest tests/test_agent.py -v
.venv/Scripts/python.exe -m pytest tests/test_robust_ops.py -v
.venv/Scripts/python.exe -m pytest tests/test_render_ops.py -v   # 「操作→成片」层
# 启动 M5 演示界面：
.venv/Scripts/python.exe -m ai_video_agent.ui     # http://127.0.0.1:7860
# 启动本地 Qwen（混合后端的离线兜底，可选）：
#   双击 E:\AI_models\Qwen_Model\start_qwen.bat   -> http://127.0.0.1:8001/v1
# 复现 M2 Paraformer 真实转写：
.venv/Scripts/python.exe scripts/paraformer_demo.py
```

**改完代码记得重启界面**：Gradio 进程不会热加载 `.py`，关掉黑窗口再双击 `一键启动界面.bat` 才会用上新代码。

> **端口占用已自愈**：若关窗后残留进程仍占着 7860，`一键启动界面.bat` 会在启动前自动清掉本项目残留 UI 进程（见第 8b 节 Bug E）；万一没清掉，`ui.py` 也会自动顺延到 7861+ 并提示，不会再崩。
> **手动排查**（可选）：`netstat -ano | findstr :7860` 看谁在占；`tasklist | findstr python` 列进程；确认是残留 UI 后可 `taskkill /PID <pid> /F`。

---

## 10. 重启提示词（直接粘到新对话框）

> 继续 AI 视频剪辑智能体项目（MVP 方案 A）。先读 `E:\多媒体融合剪辑\ai-video-agent\交付文档.md`、`M4_VERIFICATION.md`、`M2_VERIFICATION.md` 恢复上下文。当前 M1–M5 已闭环（**163 passed + 27 subtests**），Gradio UI 已交付并升级「云端优先+本地兜底」混合 LLM 后端（详见第 8 节，2026-09-23 修的实跑 bug 见第 8b 节：A 派生 id 别名表 / B JSON 容错解析 / C 提示词注入 M2 事实 / D M2 缓存与进度 / E 端口占用自愈 / F 接通真实出片 / G 音频滤镜+trim A/V 同步 / H ASS 覆盖标签 / I 输出位置契约错位 / **J 新增免 id 的 `delete_range`（根治"多区间删除被静默跳过"）** / **K 视频剪辑带动配对音频（A/V 对齐铁律）+ 多段音频改用 concat** / L 字幕去重 / **M 4 类操作「只改时间轴、不改成片」（空转）已补齐渲染：BGM 走 amix 混音、effect 走逐片段滤镜、transition 走边界 fade、move_clip 改重排语义，并新增 compile() 的 unsupported 回显与 test_render_ops 结构性护栏**，运行见第 9 节）；④「应用并编译」现已**真正调 ffmpeg 出片**（`render.py`，成片在 `outputs/<ts>/output.mp4`，界面 `gr.Video` 可预览下载）；M2 Paraformer 真实转写已跑通（`scripts/paraformer_demo.py`）。架构铁律见第 2 节，沙箱网络踩坑见第 6 节（重点：GitHub 文件 CDN 不可解析，yolov8n.pt 已落盘勿再下载；Gradio 6.27.0；FunASR 需 kaldi-native-fbank 后端 + `disable_update=True`、Paraformer 权重走 ModelScope 可达；云端 Key 真实网关=SiliconFlow，UI 默认已设；本地 Qwen3-0.6B 在 `E:\AI_models\Qwen_Model\Qwen3-0.6B`，`serve_qwen3.py`+`start_qwen.bat` 监听 8001，质量弱故仅作云端兜底）。混合策略实测定稿：云端 DeepSeek-V4-Flash 优先保正确，本地 0.6B 仅离线/断网兜底。**关键契约补充**：① `cut` 会把 clip `X` 拆成 `X__a`/`X__b` 且原 id 消失，LLM 常引用旧 id，已由 `operations.apply_operations` 的派生 id 别名表自动解析（UI 的④走这条路），别改这套命名/解析约定；② **删除/裁剪已检出内容请用 `delete_range: {start, end}`（素材绝对秒数）**，别让 LLM 推演派生 id——实测多区间时会静默丢掉一半删除；③ **A/V 对齐铁律：视频是主轨、音频跟随**（`cut`/`delete_clip`/`delete_range` 都会同步作用到同 sourceRef 的配对音频），反向不成立；④ **Gradio 的 `click(outputs=[...])` 是位置契约**，④ 的输出顺序唯一来源是 `ui.py` 的 `APPLY_OUTPUT_KEYS`，改输出组件必须同步该常量（`TestUIOutputContract` 会兜住）；⑤ **编译器必须消费操作里的全部数据**——曾出现 effect/transition/add_bgm/move_clip 四类操作「能改 Timeline、不改成片」的空转（界面还显示已应用），现已补齐：`clip.effects`→逐片段滤镜链、`tl.transitions`→边界 fade、BGM 轨（`a_bgm`）→`amix` 混音、`move_clip`→整轨重排；新增操作时务必跑 `tests/test_render_ops.py`，它的 `test_every_op_changes_compiled_output` 会强制「每个操作都必须改变编译产物」。认不出/表达不了的内容一律进 `compile()[unsupported]` 并由 ④ 回显，**禁止静默忽略**；⑥ **语义检索出的删除区间必须以「镜头」为单位**（`understand._label_ranges_from_times` 负责外扩），不能只取关键帧命中跨度 —— 否则同镜头会残留没被关键帧覆盖的画面，还可能产出零长度区间 `(t,t)` 让 `delete_range` 报错/被静默跳过。改这一段务必跑 `tests/test_agent.py::TestSemanticRemoveCoversWholeShot`；⑦ **字幕尺寸必须按 timeline 分辨率算**（`build_ass_content`）：ASS 必须显式声明 `PlayResX/PlayResY`，否则 libass 按 384×288 解释坐标、字号只跟画面高度成比例，竖屏/小分辨率会**横向溢出被裁**（`WrapStyle` 也必须是 0 才能自动折行）。改字幕样式务必跑 `tests/test_contract.py::TestSubtitleFitsFrame`；⑧ **别把 ASR 幻觉当台词，且 VAD 必须"单独实例化"才拿得到时间戳**：FunASR 对无语音/纯音乐素材会幻觉出短词。`transcribe()` 走 **VAD 分段**（`_vad_spans` → 逐段切音频逐段转写，得到**段落级**时间戳且文本顺序天然正确），VAD 不可用时退化为"整片一条"；结果过**两级护栏**：单段判据 `_looks_like_hallucination`（丢「覆盖 ≥90% 时长 + 不含汉字 + 词数 ≤ 2」）+ 整体判据 `_looks_like_silent_footage`（段数 ≥3 + 每段 ≤3 字 + 覆盖 ≥90% → 整批丢，用于拦"纯音乐被切细后逐段幻觉"）。**三个坑**：① `vad_model="fsmn-vad"` 挂在 `AutoModel` 里拿不到段边界（内部切完又拼回一条），必须**单独 `AutoModel(model="fsmn-vad")`**；② VAD 参数是**直接关键字**（`VADXOptions(**kwargs)` 消费），签名里的 `vad_post_args` **从未被使用** —— 传它会静默无效；`max_end_silence_time` 从默认 800 调到 **300** 才能把幕分干净（3 段 → 11 段，边界与幕对齐）；③ **VAD 会把纯音乐判成语音**，"有 VAD"≠"没幻觉"。句级断句仍需 `punc_model`（数百 MB，暂不引入）。改这里务必跑 `tests/test_understand.py::TestAsrHallucinationGuard` + `::TestVadSpans` + `::TestSilentFootageGuard`；⑨ **按台词删除的区间同样以「镜头」为单位**：`agent._speech_ranges` 把命中的台词段外扩到覆盖的镜头（台词粒度可到"半句"，直接删会留下半个镜头 —— 与 ⑥ 同一条铁律）；`facts_block` 里台词**按镜头聚合**后再给 LLM（`镜头 8.00-12.00s: "第三幕" / …`），让模型直接用、不要它推演外扩。指令「删掉**有**X的片段」**视觉优先、台词兜底**（解决"用户说中文『公交车』、视觉标签却是 bus"的错位）。改这里务必跑 `tests/test_agent.py::TestDeleteBySpeech`；⑩ **提示词是契约，必须被测试，且不得宣称不存在的事实**（Bug Q）：提示词内容此前**零断言**，事实块或 schema 规则被改坏时所有测试照样绿、模型开始答非所问。要有**两层**断言 —— 拼装层断言 `build_prompt()` 返回值、**线上层**让 stub 存下服务端实际收到的请求体再断言。**两个坑**：① 断言必须**切出事实段**再判（schema 的【示例】区天然含同形字符串，对整条 prompt 做 `assertNotIn` 必然误报）；② `"未按镜头聚合"` **包含** `"按镜头聚合"`，只做 `assertIn("按镜头聚合")` 会在退化场景**假通过** → 用带前缀的完整标识 + 补反向 `assertNotIn`。另外事实块有**正常/退化两个分支**，退化分支必须**自报家门**（`台词（**未按镜头聚合：…**）`），否则 schema 规则 3b（"事实里已按镜头聚合"）就在对模型说假话；规则路径的降级（拿不到镜头信息 → 按台词删除只能给 1–2s 的半句区间）也必须由 `speech_delete_degrades_to_raw_segments()` 判定并在 `do_plan` 状态里显式提示，**不许静默降级**。改这里务必跑 `tests/test_robust_ops.py::TestSpeechPromptContract` + `tests/test_ui.py::TestLLMEndToEnd::test_wire_prompt_carries_shot_aggregated_facts`；⑪ **语义类指令的答案在 M2 事实里已经写死了，别赌模型**（Bug R）：真机实测本地 Qwen3-0.6B 会**少删**（3 个只给 2 个）也会**多删**（多给一段没人要的），而这两种错误都是「操作成功、0 警告、合法 JSON」—— 容错解析 / 重试 / 别名表全都救不了。所以凡属**检索**性质的指令（删掉有/说 X 的片段），都要用 `expected_semantic_ranges()` 从 YOLO 标签 + ASR 转写算出标准答案，再用 `semantic_delete_discrepancies()` 核对模型输出，把「漏删 / 多删」显式报给用户，并**在有确定性标准答案时自动纠正为正确区间**（`do_plan` 已接：
2026-09-24 改为"能算就自动纠正、算不出才只警告"，保证交付物永远正确）。判断标准：**如果这条指令的正确答案你能用代码算出来，就不该只靠提示词去要它**。改这里务必跑 `tests/test_ui.py::TestSemanticDeleteGuard`。
