# ============================================================
# AI 视频剪辑智能体 —— 完整依赖（含 AI 模型）
# ------------------------------------------------------------
# 在 requirements.txt 基础上追加 M2 视觉理解 / 语音转写所需的重依赖：
#   torch        —— YOLOv8 推理后端
#   ultralytics  —— YOLOv8 检测（detect_objects）
#   funasr       —— Paraformer 中文语音转写
# 装好后即可启用「② M2 理解」按钮，并跑满全部 163 个测试
# （含 test_understand 里 2 个 YOLO 真实检测用例，基础依赖下会被跳过）。
#
# ⚠️ 体积/耗时提示：torch + ultralytics + funasr 合计约 2~3GB，下载较慢，
#    且首次运行还需联网下载 YOLO 权重(yolov8n.pt 等)与 Paraformer 权重。
#    仅做演示/验收用基础依赖即可；需要真实 AI 理解时再装本文件。
# ============================================================

-r requirements.txt
torch>=2.1
ultralytics>=8.1
funasr>=1.0
